You are working in the repository root of YaoYinYing/SmartX on branch codex/goal-may26-01.

Your task is to stabilize the current branch for a small PR into smartx.

Do NOT add new major features.
Do NOT build a full profile pipeline.
Do NOT build a config editor.
Do NOT split the whole ApiRequest architecture yet.
Do NOT redesign the UI broadly.

This branch should only become a safe, buildable, reviewable diagnostics/capability/profile-artifact preview.

Perform these tasks:

1. Ensure the branch compiles.
   - Check DashboardViewController usage of DashboardSubViewControllerProtocol.
   - Ensure DiagnosticsDashboardViewController conforms to DashboardSubViewControllerProtocol.
   - Implement actionSearch(string:) for DiagnosticsDashboardViewController, either by wiring it to the log search field or as a no-op with a clear comment.

2. Add a minimal SmartXRedactor.
   - Create a focused redaction utility.
   - Redact subscription URLs, URL query strings, fragments, userinfo, token-like query keys, controller secrets, and home directory paths.
   - Use it in DiagnosticsReportBuilder.
   - The diagnostics report must not output raw remote subscription URLs.
   - The diagnostics report must not output full local home paths when a "~" form is sufficient.
   - If a report is not fully sanitized, do not call it sanitized.

3. Fix DiagnosticsReportBuilder wording.
   - Rename any misleading "Generated Effective Config" label if the artifact is only a copied source config.
   - If file names are not renamed in this PR, make the displayed report text honest.
   - The report should say "Loaded Source Copy" or "Successful Reload Artifact" unless a real generated effective config pipeline exists.

4. Add or extend ProfileArtifactMetadata.
   - Include fields that make the artifact semantics explicit:
     generationMode: "source-copy"
     includesSmartXOverrides: false
     includesProfileMerge: false
     includesRuntimeOverrides: false
   - Do not claim this artifact reproduces the runtime config if SmartX runtime LightGBM overrides are not written into the file.

5. Protect dangerous diagnostics actions.
   - Add confirmation dialogs for restart core, update dashboard assets, update GEO assets, run debug GC, and restore last-known-good if not already confirmed.
   - Keep read-only diagnostics easy to use.
   - Put destructive or maintenance actions behind clear wording.

6. Fix obvious SmartX identity text in newly touched UI.
   - Replace "Use ClashX LightGBM Settings" with "Use SmartX LightGBM Override" or equivalent.
   - Replace new user-facing "ClashX" text with "SmartX" unless it is explicitly legacy compatibility text.
   - Do not perform a full bundle/helper identity migration in this PR.

7. Fix the structured log level bug.
   - StructedLogReq currently accepts a level parameter but uses a fixed logLevel.
   - Store the initializer argument and use it when building the websocket URL.
   - Do not rename files unless necessary for compilation.

8. Reduce report/log UI risk without major rewrite.
   - Avoid reading enormous log files synchronously if easy to fix.
   - At minimum, cap the displayed log lines and document that full tail-reading should be implemented in a later PR.
   - Do not build a full log subsystem in this PR.

9. Adjust capability naming only if low-risk.
   - If memoryStream is only an HTTP /memory snapshot, rename it to memorySnapshot.
   - Update call sites accordingly.
   - Do not redesign the whole capability system in this PR.

10. Add or update lightweight tests if the repository already has a test path suitable for these changes.
   - At minimum, add tests or documented fixtures for SmartXRedactor if feasible.
   - If test infrastructure blocks direct testing, document the gap in docs/memory and do not copy production logic into another harness.

After changes, run or document the intended commands:

- git status
- xcodebuild build command for the existing workspace and scheme if available
- any existing Swift test or harness command
- grep checks for raw "Use ClashX LightGBM Settings"
- grep checks for "sanitized diagnostics" wording

Prepare a PR summary with:

- What this PR adds
- What it intentionally does not solve
- Known limitations
- Follow-up PR list

Merge criteria for codex/goal-may26-01:

- The branch compiles.
- DiagnosticsDashboardViewController conforms to DashboardSubViewControllerProtocol.
- Copy Report no longer exports raw remote subscription URLs.
- Dangerous diagnostics actions require confirmation.
- Profile artifacts are honestly labeled as source-copy artifacts, not true generated effective configs.
- New user-facing LightGBM text uses SmartX identity, not ClashX identity.
- The structured log level initializer bug is fixed.
- The PR description explicitly states that endpoint architecture, override pipeline, TUN lifecycle, and config workspace remain future work.