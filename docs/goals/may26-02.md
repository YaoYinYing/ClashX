You are working in the repository root of YaoYinYing/SmartX.

Create a focused PR from smartx.

PR title:
User-facing SmartX identity cleanup

Goal:
Replace user-facing ClashX identity with SmartX and remove stale links to deleted/original ClashX/Clash repositories, while preserving legacy bundle/helper identifiers for a later signing migration.

This PR is about visible product identity and documentation cleanup.
It must NOT perform full bundle/helper/signing migration.

Do NOT:
- change helper bundle identifier
- change helper Mach service name
- change SMPrivilegedExecutables
- change SMAuthorizedClients
- change Team ID requirements
- change Sparkle signing keys
- change iCloud container identifier unless you create a migration plan
- remove clash:// compatibility
- rename the whole Xcode project directory
- rewrite AppDelegate
- implement TUN lifecycle
- implement SmartX override layer
- implement generated effective config

Tasks:

1. App display name and product name
   - Inspect Xcode build settings and Info.plist.
   - Change the app display name shown to users from ClashX to SmartX.
   - Prefer setting CFBundleDisplayName to SmartX if PRODUCT_NAME is still legacy.
   - If changing PRODUCT_NAME is low-risk, change the main app target product name to SmartX.
   - Do not rename helper product names in this PR.
   - Do not break the build.

2. Info.plist user-facing text
   Update user-visible strings:
   - NSLocationAlwaysAndWhenInUseUsageDescription
   - NSLocationWhenInUseUsageDescription
   - NSHumanReadableCopyright
   - any visible URL type names if appropriate

   Replace wording like:
   "ClashX use location info..."
   with:
   "SmartX uses location information to detect the current Wi-Fi SSID and provide auto-suspend behavior."

   Preserve technical legacy keys if needed, but add comments or documentation that legacy bundle/helper identities are not completed in this PR.

3. URL schemes
   - Keep clash:// for compatibility.
   - Keep clashx:// only if needed for compatibility.
   - Add smartx:// if feasible and low-risk.
   - Update README to document compatibility:
     - smartx:// is preferred for SmartX
     - clash:// and/or clashx:// may remain legacy-compatible import schemes
   - Do not remove legacy schemes in this PR.

4. README cleanup
   Rewrite README top identity:
   - title should be SmartX
   - remove remote logo reference to Dreamacro/clash raw URL
   - use local documentation image if available, or omit the logo
   - describe SmartX as a macOS native client derived from ClashX and integrating the Vernesong Smart fork of mihomo
   - explicitly state it is not official ClashX / ClashX Pro
   - explicitly state it does not provide proxy servers or subscriptions

   Remove or replace stale links to:
   - deleted Dreamacro/clash original repository
   - deleted yichengchen/ClashX original repository
   - old AppCenter release links
   - ClashX Pro legacy release wording

   Do not claim production release readiness.

5. README build requirement update
   Ensure README says:
   - use project Ruby, recommended Ruby 3.2.4
   - system Ruby 2.6 is unsupported
   - Ruby 4 is currently unsupported for the locked Bundler/CocoaPods toolchain
   - use rbenv local 3.2.4
   - run bundle install before install_dependency.sh if needed

6. User-facing in-app text search
   Search for visible strings containing:
   - "ClashX"
   - "clashx"
   - "ClashX Pro"
   - "west2online"
   - "yichengchen"
   - "Dreamacro"
   - old deleted repo URLs

   Replace only user-facing text with SmartX wording.
   Leave source file headers and internal comments alone unless they appear in UI, README, docs, or generated diagnostics.

7. Diagnostics/report wording
   Ensure diagnostics report says SmartX consistently.
   If any copied report text still says ClashX as product name, replace it.
   Do not change internal technical names if they describe legacy paths or compatibility behavior.

8. Menu and preference wording
   Search for menu labels and settings descriptions that mention ClashX.
   Replace visible text:
   - "Change your status menu icon ... restart ClashX" -> "restart SmartX"
   - "关闭ClashX的通知" -> "关闭 SmartX 的通知"
   - "clashX menu icon" -> "SmartX menu icon"
   - "Using the last config known by ClashX" -> "Using the last config known by SmartX"

9. Legacy identity documentation
   Add or update a short section in README:

   "Legacy identity note"
   Explain:
   - Some internal bundle/helper identifiers may still use ClashX-era values.
   - These are retained temporarily to avoid breaking helper installation, URL import, and config migration.
   - A later signing migration PR will replace bundle IDs, helper ID, Mach service name, iCloud container, and SMJobBless requirements.
   - User-facing branding is SmartX.

10. Memory docs
   Update docs/memory if present:
   - docs/memory/10-roadmap.md
   - docs/memory/09-security-signing-and-license.md
   - docs/memory/14-review-findings-and-patchwork-risks.md

   Document that this PR completes user-facing identity cleanup but does not complete signing identity migration.

11. Grep validation
   Run or document these searches:

   grep -Rni "Dreamacro/clash" README.md docs ClashX || true
   grep -Rni "yichengchen" README.md docs ClashX || true
   grep -Rni "AppCenter" README.md docs ClashX || true
   grep -Rni "ClashX Pro" README.md docs ClashX || true
   grep -Rni "ClashX" README.md docs ClashX | head -n 100

   Remaining "ClashX" hits must be classified as:
   - internal legacy identifier
   - source file header
   - compatibility scheme
   - old config path compatibility
   - or still needing cleanup

12. Build validation
   Run if feasible:

   xcodebuild \
     -workspace ClashX.xcworkspace \
     -scheme ClashX \
     -configuration Debug \
     CODE_SIGNING_ALLOWED=NO \
     CODE_SIGNING_REQUIRED=NO \
     build

   If the scheme is still named ClashX, do not rename it in this PR unless trivial.
   Record that scheme renaming is separate.

PR body must include:

## What changed
- User-facing product name cleaned up to SmartX.
- README no longer points to deleted original Clash/ClashX repositories.
- Permission strings and user docs now use SmartX.
- Legacy URL scheme compatibility is documented.
- Legacy signing/helper identifiers remain unchanged.

## What this PR does not do
- Does not migrate bundle identifiers.
- Does not migrate helper identity or SMJobBless requirements.
- Does not migrate iCloud container.
- Does not complete public release signing.
- Does not rename all source directories or Xcode schemes.

## Remaining known legacy names
List remaining ClashX occurrences and why each category remains.