You are working in YaoYinYing/SmartX.

Problem:
install_dependency.sh fails on macOS system Ruby 2.6 because Gemfile pins activesupport 7.0.8, which requires Ruby >= 2.7. The script currently reaches bundle install and fails with a Bundler dependency error.

Goal:
Make the Ruby version requirement explicit and fail early with a helpful message.

Do not downgrade activesupport.
Do not downgrade CocoaPods.
Do not remove fastlane.
Do not modify Podfile.
Do not change dashboard branding logic except if needed to keep script order clean.

Tasks:

1. Add a .ruby-version file at repo root.
   Use Ruby 3.2.4 unless the project already documents another modern Ruby version.

2. Update install_dependency.sh.
   Before "bundle install", check the active Ruby version.
   Require Ruby >= 2.7.0 at minimum.
   Prefer recommending Ruby 3.2.4.

3. If Ruby is too old, print a clear error such as:

   SmartX requires Ruby >= 2.7 for Bundler dependencies.
   Your Ruby is: <version>
   macOS system Ruby 2.6 is not supported.
   Install a project Ruby, for example:
     brew install rbenv ruby-build
     rbenv install 3.2.4
     rbenv local 3.2.4
     gem install bundler
   Then rerun:
     bundle install
     bash install_dependency.sh

4. Implement the version check in portable bash.
   It can call Ruby itself:

   ruby -e 'exit Gem::Version.new(RUBY_VERSION) >= Gem::Version.new("2.7.0") ? 0 : 1'

5. Keep the error before bundle install so users fail early.

6. Update README or docs/memory build notes if a build/dependency doc exists.
   Mention:
   - system Ruby 2.6 is not supported
   - project Ruby 3.2.4 is recommended
   - run bundle install using the project Ruby

7. Do not claim the whole release pipeline is fixed.