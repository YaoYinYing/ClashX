You are working in the repository root of YaoYinYing/SmartX.

Base branch: the current PR branch if PR #9 is still open, otherwise create a new branch from smartx.

Goal:
Update GitHub Actions so every pull request builds and uploads unsigned SmartX app artifacts, including a Legacy macOS 10.14 deployment-target build.

This task is CI/build-system only.

Do NOT modify application runtime code unless absolutely required for build settings.
Do NOT change helper signing identity.
Do NOT implement release notarization.
Do NOT implement SmartX managed overrides, generated effective config, TUN lifecycle, or config workspace.
Do NOT remove the current modern build unless replacing it with an equivalent matrix entry.

Background:
GitHub-hosted runners do not provide a real macOS 10.14 runner. The goal is to compile a Legacy artifact with MACOSX_DEPLOYMENT_TARGET=10.14 using Xcode 16.4 on macos-15. This validates the legacy deployment-target compile path, not runtime behavior on a real 10.14 machine.

Required CI design:

1. PR CI must build at least two unsigned app artifacts:

   Legacy:
   - runner: macos-15
   - Xcode: 16.4
   - Go: 1.21.x
   - Ruby: 3.2
   - MACOSX_DEPLOYMENT_TARGET=10.14
   - configuration: Debug
   - artifact name should include Legacy, macOS10.14, PR number, and commit SHA

   Modern:
   - runner: macos-26
   - Xcode: 26.3 or the existing modern Xcode version used by the project
   - Go: 1.21.x
   - Ruby: 3.2
   - MACOSX_DEPLOYMENT_TARGET=11.0 or project default if safer
   - configuration: Debug
   - artifact name should include Modern, PR number, and commit SHA

2. Avoid duplicate PR workflows.

   Current repository may have both:
   - .github/workflows/main.yml
   - .github/workflows/pr-ci.yml

   Make PR behavior clear. Prefer this structure:

   - pr-ci.yml:
     runs pull_request validation and uploads PR artifacts.
     It may use a matrix for Legacy and Modern builds.

   - main.yml:
     handles release/tag/manual unsigned release artifact builds only.
     Remove pull_request trigger from main.yml, unless there is a strong reason to keep a second PR artifact workflow.

   If keeping both PR workflows, rename/document them so their roles are explicit:
   - PR Validation Gate
   - PR Debug Artifact Build

   Prefer one PR workflow to avoid confusion.

3. Add matrix fields.

   Suggested matrix:

   strategy:
     fail-fast: false
     matrix:
       include:
         - flavor: legacy
           display_name: Legacy macOS 10.14
           runner: macos-15
           xcode: "16.4"
           deployment_target: "10.14"
           configuration: Debug
           artifact_suffix: Legacy-macOS10.14-Debug

         - flavor: modern
           display_name: Modern
           runner: macos-26
           xcode: "26.3"
           deployment_target: "11.0"
           configuration: Debug
           artifact_suffix: Modern-Debug

4. Each matrix job must:

   - checkout repository
   - setup Go 1.21.x
   - setup Ruby 3.2 with bundler cache
   - select the requested Xcode version
   - print toolchain versions:
     xcodebuild -version
     xcrun --sdk macosx --show-sdk-version
     swift --version
     go version
     ruby --version
     bundle --version
   - run bash install_dependency.sh
   - resolve SPM dependencies
   - run helper Release requirement check at least once, preferably in a separate non-matrix validation job or in the legacy job
   - run existing security harness
   - run endpoint builder smoke harness
   - run capability cache identity smoke harness
   - build unsigned Debug app
   - package SmartX.app into a zip
   - upload the zip and logs as artifacts

5. Build command.

   Use xcodebuild roughly like:

   xcodebuild \
     -workspace ClashX.xcworkspace \
     -scheme ClashX \
     -configuration "${BUILD_CONFIGURATION}" \
     -destination 'platform=macOS' \
     -derivedDataPath ".derivedData-${FLAVOR}" \
     MACOSX_DEPLOYMENT_TARGET="${MACOSX_DEPLOYMENT_TARGET}" \
     CODE_SIGNING_ALLOWED=NO \
     CODE_SIGNING_REQUIRED=NO \
     CODE_SIGN_IDENTITY="" \
     build

   Important:
   The Legacy job must explicitly pass:
   MACOSX_DEPLOYMENT_TARGET=10.14

6. Artifact packaging.

   After build, locate SmartX.app under:

   .derivedData-${FLAVOR}/Build/Products/${BUILD_CONFIGURATION}/SmartX.app

   If not found, fail the job and upload logs.

   Package with:

   ditto -c -k --sequesterRsrc --keepParent "$APP_PATH" ".ci-artifacts/SmartX-${ARTIFACT_SUFFIX}-${GITHUB_SHA}.app.zip"

   Upload using actions/upload-artifact.

   Artifact name must be unique per matrix job, for example:

   SmartX-${{ matrix.artifact_suffix }}-PR-${{ github.event.pull_request.number || 'manual' }}-${{ github.sha }}

   Include:
   - app zip
   - .ci-logs
   - xcodebuild logs
   - toolchain logs

   Set:
   if-no-files-found: error
   retention-days: 14

7. Do not upload sensitive files.

   Do not include:
   - DerivedData wholesale
   - provisioning profiles
   - keychains
   - certificates
   - secrets
   - full home directory paths beyond build logs already expected

8. Add smoke harnesses to CI.

   Run these commands or equivalent:

   swift Tests/SecurityHarness/security_harness.swift

   swiftc \
     -module-cache-path /private/tmp/swift-module-cache \
     ClashX/General/Utils/ControllerEndpointBuilder.swift \
     Tests/SecurityHarness/controller_endpoint_builder_smoke.swift \
     -o /tmp/controller-endpoint-builder-smoke
   /tmp/controller-endpoint-builder-smoke

   swiftc \
     -module-cache-path /private/tmp/swift-module-cache \
     ClashX/General/Utils/ControllerEndpointBuilder.swift \
     ClashX/General/Managers/CoreCapability.swift \
     Tests/SecurityHarness/capability_cache_identity_smoke.swift \
     -o /tmp/capability-cache-identity-smoke
   /tmp/capability-cache-identity-smoke

9. Update Tests/README.md.

   State:
   - PR CI now uploads unsigned Debug artifacts for Legacy and Modern builds.
   - Legacy artifact means deployment-target compile check for macOS 10.14, not actual runtime testing on macOS 10.14.
   - Modern artifact validates the current modern SDK build line.
   - Smoke harnesses now run in CI.
   - True macOS 10.14 runtime validation still requires a real 10.14 machine or VM outside GitHub-hosted runners.

10. Update docs/memory/02-build-and-release.md and docs/memory/13-test-strategy-and-legacy-hardening.md.

   Add:
   - Legacy CI lane: macos-15 + Xcode 16.4 + MACOSX_DEPLOYMENT_TARGET=10.14.
   - Modern CI lane: macos-26 + Xcode 26.x.
   - Every PR uploads unsigned artifacts.
   - Artifact upload does not imply signing, notarization, helper installation, or runtime compatibility.
   - Legacy compile success does not prove real 10.14 runtime success.

11. PR body.

   Prepare a PR summary:

   ## What changed
   - Added PR build matrix for Legacy and Modern artifacts.
   - Legacy lane compiles with Xcode 16.4 and MACOSX_DEPLOYMENT_TARGET=10.14.
   - Modern lane compiles with Xcode 26.x.
   - Every PR uploads unsigned SmartX.app zip artifacts and logs.
   - Endpoint builder and capability identity smoke harnesses now run in CI.
   - main.yml/pr-ci.yml trigger duplication was cleaned or explicitly documented.

   ## What this does not prove
   - It does not prove runtime behavior on macOS 10.14.
   - It does not prove helper installation.
   - It does not prove signing/notarization.
   - It does not produce release-ready binaries.

   ## Manual checks
   Include the workflow file changed and commands run locally if any.

Acceptance criteria:

- PR CI has a Legacy macOS10.14 deployment-target build.
- PR CI has a Modern build.
- Both upload artifacts on every PR.
- Artifact names are unique per matrix job.
- Smoke harnesses run in CI, not only documented.
- main.yml and pr-ci.yml no longer create confusing duplicate PR behavior.
- Docs explicitly distinguish compile-target validation from runtime validation.