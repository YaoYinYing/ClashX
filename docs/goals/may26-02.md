You are working in the repository root of YaoYinYing/SmartX.

Important:
Work directly on the current branch. Do NOT create a new branch.

Goal:
Finish the user-visible SmartX identity cleanup for the current branch.

The app still appears as "ClashX" in some build/runtime contexts because the Xcode product name, bundle name, README, docs, scripts, and some references still inherit ClashX identity. Some content also points to the original ClashX repositories that are deleted or no longer authoritative.

This task should update user-facing product identity and documentation links, but it must NOT perform a risky low-level identity migration.

============================================================
SCOPE
============================================================

Do change:
- User-facing app name.
- Build product name if safe.
- CFBundleName / CFBundleDisplayName behavior.
- README title and description.
- User-visible UI strings newly owned by SmartX.
- Documentation references that still point to deleted original ClashX repositories.
- Script comments and messages that say "Build Clash core" or imply this is still original ClashX.
- About/copyright strings if they still say ClashX as the product name.
- Memory docs where current status is outdated.

Do NOT change in this PR:
- Bundle identifier.
- Helper bundle identifier.
- SMJobBless / SMPrivilegedExecutables / SMAuthorizedClients signing requirements.
- iCloud container identifiers.
- URL schemes for legacy compatibility.
- Target names if changing them causes large Xcode project churn.
- Source directory name ClashX/.
- Scheme name unless trivial and safe.
- Helper executable name.
- Existing compatibility text that explicitly says legacy ClashX compatibility.
- Historical attribution lines that accurately say SmartX is derived from ClashX.

Reason:
Low-level identity migration requires a dedicated signing/helper/iCloud migration PR. This PR is only user-visible product identity cleanup.

============================================================
PART 1 — Inspect current identity
============================================================

Inspect these files first:

- ClashX/Info.plist
- ClashX.xcodeproj/project.pbxproj
- README.md
- install_dependency.sh
- Gemfile / fastlane if relevant
- docs/memory/*.md
- ClashX/**/*.swift
- ClashX/**/*.xib
- ClashX/**/*.storyboard
- ProxyConfigHelper/*.plist
- fastlane/*
- .github/workflows/*

Run searches:

grep -Rni "ClashX" README.md docs ClashX ProxyConfigHelper fastlane .github install_dependency.sh Gemfile Podfile 2>/dev/null || true
grep -Rni "yichengchen\\|Dreamacro\\|west2online\\|bannedbook\\|ClashX-Pro/ClashX\\|github.com/.*/ClashX" README.md docs ClashX fastlane .github install_dependency.sh 2>/dev/null || true

Classify every remaining "ClashX" occurrence into one of these categories:

A. Must rename now:
   User-facing product name, README title, new SmartX UI text, build script messages, diagnostics labels, release notes, app name.

B. Keep as historical attribution:
   "Derived from ClashX", "legacy ClashX compatibility", copyright history, source path comments.

C. Keep as technical legacy identity:
   Bundle identifier, helper identifier, SMJobBless requirements, iCloud container, URL scheme compatibility, source directory, target name if too risky.

D. Needs follow-up:
   Anything that should change later but not in this PR.

============================================================
PART 2 — Fix app display/product name
============================================================

Current Info.plist has:
- CFBundleDisplayName = SmartX
- CFBundleName = $(PRODUCT_NAME)

If the built app still appears as ClashX, the PRODUCT_NAME is probably still ClashX.

Update Xcode build settings for the main app target so the built product is SmartX.app.

Preferred change:
- Set PRODUCT_NAME = SmartX for the main app target build configurations.

Do not change helper PRODUCT_NAME.
Do not change helper identifiers.
Do not change product bundle identifier unless already configured safely.
Do not rename source directories.

Also consider setting CFBundleName explicitly to SmartX if safer than changing target PRODUCT_NAME. But if PRODUCT_NAME can be safely changed for the app target, prefer PRODUCT_NAME = SmartX.

After the change, the built app should appear as:
SmartX.app

Important:
If changing PRODUCT_NAME breaks references in Copy Files phases, helper embedding, build scripts, or app paths, keep PRODUCT_NAME unchanged and instead set CFBundleName to SmartX. Document why. But first try the clean product-name change for the main app only.

============================================================
PART 3 — Clean README and deleted upstream links
============================================================

Update README.md to reflect SmartX identity.

The README should say:
- SmartX is a macOS native mihomo/smart client derived from ClashX.
- It integrates Vernesong mihomo smart core through the current go module replacement.
- It is experimental / preview unless release readiness is proven.
- It does not provide proxy services.
- It keeps legacy compatibility with ClashX/clash URL schemes where needed.
- It is not the original ClashX project and does not claim to be official ClashX Pro.

Remove or replace links to deleted or non-authoritative original ClashX repositories.

If historical attribution is needed, write it as text, not as a primary project link:
"SmartX is derived from the ClashX community codebase."

If you need repository links, point to:
- Current repository: https://github.com/YaoYinYing/SmartX
- mihomo upstream if already referenced correctly
- Vernesong smart fork if already referenced correctly
- MetaCubeX/Yacd-meta only for embedded dashboard resource if relevant

Do not add unsupported claims such as "stable release", "notarized", or "production ready".

============================================================
PART 4 — Clean build script identity
============================================================

Update install_dependency.sh messages and comments.

Examples:
- "Build Clash core" should become "Build SmartX embedded mihomo core" or "Build embedded mihomo smart core".
- "install dashboard" can stay, but mention SmartX dashboard branding if that logic exists.
- Do not rename actual paths unless needed.

If the script has Ruby version checks, keep them.
If it does not, do not add a large Ruby toolchain patch unless it is already part of this branch's scope. A small message improvement is fine.

============================================================
PART 5 — Clean user-facing Swift strings
============================================================

Search for user-visible "ClashX" strings.

Rename these to "SmartX" when they refer to the product.

Examples:
- "Use ClashX LightGBM Settings" -> "Use SmartX LightGBM Override"
- "Using the last config known by ClashX" -> "Using the last config known by SmartX"
- "ClashX Diagnostics" -> "SmartX Diagnostics"
- "ClashX config" -> "SmartX config" if user-visible and owned by this app

Do not rename:
- legacy compatibility strings that explicitly mention clashx:// or original compatibility.
- copyright comments in old source headers.
- technical bundle IDs or helper requirements in this PR.

============================================================
PART 6 — Clean docs/memory
============================================================

Update memory docs only where they are now misleading.

The docs should consistently say:
- Product name: SmartX
- Current source directory/target may still contain ClashX legacy naming.
- Low-level identity migration is not complete.
- Bundle ID/helper/iCloud migration remains future work.
- Legacy "ClashX" references in source paths do not mean the product is still ClashX.

If docs/memory/14-review-findings-and-patchwork-risks.md exists, add a short note:
"This branch cleaned user-visible SmartX identity, but low-level bundle/helper/iCloud identity remains a future dedicated migration."

============================================================
PART 7 — Verification
============================================================

Run or attempt these checks:

grep -Rni "ClashX" README.md docs ClashX ProxyConfigHelper fastlane .github install_dependency.sh 2>/dev/null || true

Review remaining matches manually and classify them:
- legacy technical identity
- historical attribution
- source path/header comment
- follow-up

Run:

grep -Rni "yichengchen\\|Dreamacro\\|west2online\\|bannedbook\\|github.com/.*/ClashX" README.md docs ClashX fastlane .github install_dependency.sh 2>/dev/null || true

Ensure deleted upstream repositories are not presented as current project links.

If possible, run:

xcodebuild \
  -workspace ClashX.xcworkspace \
  -scheme ClashX \
  -configuration Debug \
  CODE_SIGNING_ALLOWED=NO \
  CODE_SIGNING_REQUIRED=NO \
  -quiet build

After build, inspect the product name:
- It should produce or display SmartX.app if PRODUCT_NAME was changed.
- If the target/scheme remains ClashX, document that this is legacy build-system naming and not the user-facing product name.

============================================================
PART 8 — PR summary
============================================================

Prepare a summary:

## What changed
- Updated user-visible app identity to SmartX.
- Changed main app product/display naming where safe.
- Cleaned README and stale upstream ClashX links.
- Updated build script messages and SmartX-owned user-facing strings.
- Documented remaining low-level legacy identity.

## What did not change
- Bundle IDs.
- Helper IDs.
- SMJobBless requirements.
- iCloud containers.
- Source directory name.
- Legacy URL scheme compatibility.

## Why
SmartX should no longer present itself as ClashX, but low-level signing/helper/iCloud migration must be handled in a separate dedicated PR.

============================================================
BOUNDARIES
============================================================

Do not create a new branch.
Do not perform broad file moves.
Do not rename the ClashX source directory.
Do not rewrite the Xcode project unnecessarily.
Do not change helper signing metadata.
Do not change bundle identifiers.
Do not remove legacy URL schemes.
Do not remove historical attribution.
Do not claim public release readiness.