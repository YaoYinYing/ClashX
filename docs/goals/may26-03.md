You are working in the repository root of YaoYinYing/SmartX on branch codex/smartx-foundation-hardening-01.

A draft PR has already been opened:

PR #10: SmartX foundation hardening: override, TUN lifecycle, validators, diagnostics

Your task is to make this PR ready for review by fixing the most important remaining gap.

Do NOT expand the PR scope.
Do NOT implement generated effective config.
Do NOT implement Config Workspace.
Do NOT redesign Diagnostics Dashboard.
Do NOT split ApiRequest.
Do NOT change app/helper identity.
Do NOT add new product features.

Focus only on CI/test wiring and small correctness fixes needed for this PR.

Context:

This branch already added:

- SmartXManagedOverrideManager
- LightGBMSettingsViewModel
- TunConfigValidator
- DNSConfigValidator
- TunLifecycleCoordinator
- DiagnosticsLogReader
- Profile artifact terminology cleanup
- several new smoke harnesses

The main issue found in review is:

The new smoke harnesses exist, but pr-ci.yml does not run them yet.

Current pr-ci.yml runs only:

- Tests/SecurityHarness/security_harness.swift
- Tests/SecurityHarness/controller_endpoint_builder_smoke.swift
- Tests/SecurityHarness/capability_cache_identity_smoke.swift

But this branch also adds:

- Tests/SecurityHarness/config_validator_smoke.swift
- Tests/SecurityHarness/diagnostics_log_reader_smoke.swift
- Tests/SecurityHarness/profile_artifact_metadata_smoke.swift
- Tests/SecurityHarness/smartx_managed_override_smoke.swift

These new harnesses must be wired into CI before PR #10 is ready.

Tasks:

1. Update .github/workflows/pr-ci.yml

Add CI steps to compile and run the new smoke harnesses.

They should run in the validation-gate job.

They should also run in the pr-artifacts matrix job if that job already repeats smoke harnesses.

Use the same style as the existing smoke harness steps.

2. Compile each harness with the minimum required production files.

For config_validator_smoke.swift, compile with:

- ClashX/General/Utils/ConfigValidationIssue.swift
- ClashX/General/Utils/TunConfigValidator.swift
- ClashX/General/Utils/DNSConfigValidator.swift
- Tests/SecurityHarness/config_validator_smoke.swift

For diagnostics_log_reader_smoke.swift, compile with:

- ClashX/General/Utils/DiagnosticsLogReader.swift
- Tests/SecurityHarness/diagnostics_log_reader_smoke.swift

For profile_artifact_metadata_smoke.swift, inspect what production files it needs and compile only those files plus the harness.

Likely candidates:

- ClashX/General/Managers/ProfileArtifactManager.swift
- Tests/SecurityHarness/profile_artifact_metadata_smoke.swift

If ProfileArtifactManager depends on unavailable app types, do not copy production logic into the harness. Instead, adjust the harness or production code minimally so the harness can call the real metadata type.

For smartx_managed_override_smoke.swift, inspect dependencies carefully.

It may need:

- ClashX/General/Managers/SmartXManagedOverrideManager.swift
- Tests/SecurityHarness/smartx_managed_override_smoke.swift

The harness currently provides lightweight stand-ins for Logger, Settings, and Paths. That is acceptable as a temporary smoke harness only if it directly compiles the production SmartXManagedOverrideManager.swift and does not copy its implementation.

3. Avoid copied logic.

Do not duplicate validator, override, artifact, or log-reader production implementation inside harness files.

Harnesses may provide minimal stubs for unrelated app infrastructure such as Logger, Settings, Paths, or ClashConfig when needed for standalone compilation.

4. Update Tests/README.md

Add a concise section listing the new smoke harnesses and explaining:

- which production files they compile
- what behavior they cover
- that they are temporary smoke checks, not a replacement for a real XCTest target

5. Run or document these checks:

- bash scripts/codex-lint-scripts.sh
- bash scripts/codex-test-focused.sh, if feasible
- bash scripts/codex-diagnose-project.sh, if feasible
- the individual swiftc commands for the new smoke harnesses
- grep -R "Use ClashX LightGBM Settings" ClashX || true
- grep -R "Generated Effective Config" ClashX docs || true

If a command fails because of environment/toolchain limits, record the exact failure. Do not claim success.

6. Update PR #10 description if needed.

Add a short note that the new smoke harnesses are now wired into CI.

Keep the PR draft until CI has run.

Acceptance criteria:

- pr-ci.yml runs config_validator_smoke.swift.
- pr-ci.yml runs diagnostics_log_reader_smoke.swift.
- pr-ci.yml runs profile_artifact_metadata_smoke.swift.
- pr-ci.yml runs smartx_managed_override_smoke.swift.
- The new harnesses compile production code, not copied implementation logic.
- Tests/README.md documents the new smoke checks.
- No new product scope is added.
- No new patchwork UI or endpoint code is added.

Important review doctrine:

This PR is foundation hardening. It must reduce risk, not add surface area.