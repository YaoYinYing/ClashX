You are working in the YaoYinYing/SmartX repository.

Your task is to create ONE consolidated CI stabilization PR.

This PR must fix the currently failing CI for the SmartX foundation-hardening work. Do not add product features. Do not expand SmartX architecture unless needed to make CI and test harnesses honest.

Primary branch situation:

- smartx is the base branch.
- codex/foundation-hardening-may26 contains the foundation-hardening code.
- codex/plan-doc-may26 may contain only planning docs.
- PR #11 may currently point to codex/plan-doc-may26 even though its title describes foundation hardening.

Your first job is to verify branch/PR alignment. If the open PR points at the wrong head branch, do not silently ignore it. Record that clearly in the final response.

Preferred working branch:

codex/ci-stabilize-foundation-may26

Start from codex/foundation-hardening-may26 if it exists. If you are already on that branch, create the stabilization branch from it. If it does not exist locally, fetch it. If unavailable, start from smartx and document the limitation.

Hard non-goals:

- Do NOT add a config editor.
- Do NOT implement a full generated effective config pipeline.
- Do NOT implement embedded-core TUN.
- Do NOT migrate bundle/helper identity.
- Do NOT add user-facing multi-core selection.
- Do NOT rewrite Diagnostics Dashboard.
- Do NOT rewrite ApiRequest beyond what is necessary for CI.
- Do NOT make CI green by deleting meaningful tests.
- Do NOT make smoke harnesses green by copying production logic into tests.
- Do NOT claim release readiness, signing readiness, notarization readiness, or high test coverage.

Core goal:

Make CI reliable enough that the foundation-hardening branch can be reviewed. The CI should fail only on real problems, not on broken harness wiring, missing compile dependencies, wrong branch targeting, unavailable runner assumptions, or misleading scripts.

Execution discipline:

This task must not stop early.

If one command fails:
- record the command
- record the likely cause
- continue with the remaining investigation and fixes

If a file is missing:
- search for equivalent files
- adapt the fix
- document the difference

If GitHub CLI is unavailable:
- do local branch and workflow checks
- document that remote PR/head verification could not be automated

If a harness cannot honestly call production code:
- either restructure small production models into pure files so it can
- or remove that harness from required CI and document why
- do NOT copy production logic into the harness just to make CI pass

Final response must include a CI Failure Ledger and a Fix Ledger.

============================================================
PHASE 0 — Branch and PR alignment check
============================================================

Run:

git status --short
git branch --show-current
git log --oneline --decorate -8
git branch -a | sed -n '1,120p'

If gh is available, also run:

gh pr list --state open --json number,title,headRefName,baseRefName,isDraft,statusCheckRollup
gh pr view 11 --json number,title,headRefName,baseRefName,isDraft,statusCheckRollup || true

Record:

- current branch
- intended base
- whether PR #11, if present, points to codex/foundation-hardening-may26 or codex/plan-doc-may26
- whether CI being inspected belongs to the correct head branch

If PR #11 points to codex/plan-doc-may26, do not try to hide this. State that the CI/PR head is misaligned and the review branch must be updated or a new PR must be opened from codex/foundation-hardening-may26 / codex/ci-stabilize-foundation-may26.

Do not merge anything.

============================================================
PHASE 1 — Reproduce CI steps locally as much as possible
============================================================

Inspect:

.github/workflows/pr-ci.yml
.github/workflows/post-merge-dmg.yml
scripts/codex-test-focused.sh
scripts/codex-lint-scripts.sh
scripts/codex-diagnose-project.sh
scripts/codex-build-debug.sh
Tests/README.md
Tests/SecurityHarness/

Run these local checks, but continue after failures:

bash scripts/codex-lint-scripts.sh
bash scripts/codex-test-focused.sh
bash scripts/codex-diagnose-project.sh

If possible:

bash scripts/codex-build-debug.sh

Also run direct smoke compile inventory manually or through a script:

- controller endpoint builder smoke
- capability cache identity smoke
- config validator smoke
- diagnostics log reader smoke
- diagnostics artifact formatter smoke
- profile artifact metadata smoke
- smartx managed override smoke
- effective config generator smoke
- remote config decode smoke
- redactor smoke
- security harness

Create a temporary local log directory if needed:

.ci-local-logs/

Do not stop after the first failure. Collect all failing harnesses.

For every failing harness, classify the failure:

A. Missing production dependency in swiftc command
B. Production file imports app-only frameworks and cannot be standalone-compiled
C. Harness copied logic instead of calling production code
D. Real production compile error
E. Workflow runner/toolchain issue
F. Workspace/CocoaPods/SPM issue
G. Signing/helper issue
H. Unknown

============================================================
PHASE 2 — Fix broken standalone smoke harness wiring
============================================================

Problem pattern:
The workflow uses swiftc with isolated production files. This is fragile.

For each smoke harness in .github/workflows/pr-ci.yml and scripts/codex-test-focused.sh:

1. Inspect the swiftc command.
2. Inspect every production file passed to swiftc.
3. Check whether that file references types/functions not included in the command.
4. Fix the dependency list or extract pure shared models.

Known high-risk example:

EffectiveConfigGenerator.swift references SmartXManagedOverride.
The workflow currently compiles only:
  ClashX/General/Managers/EffectiveConfigGenerator.swift
  Tests/SecurityHarness/effective_config_generator_smoke.swift

This likely fails unless the harness defines a duplicate SmartXManagedOverride.

Preferred fix:
- Extract pure Codable models used by both EffectiveConfigGenerator and SmartXManagedOverrideManager into a new pure Swift file, for example:
  ClashX/General/Models/SmartXManagedOverrideModel.swift
  or
  ClashX/Models/SmartXManagedOverrideModel.swift
- The file should contain only pure Foundation-compatible data types:
  SmartXManagedOverride
  LightGBMOverride
  possibly small generation/provenance structs if needed
- Update SmartXManagedOverrideManager.swift and EffectiveConfigGenerator.swift to use those shared models.
- Update project.pbxproj so the app target includes the new file.
- Update workflow swiftc commands and scripts to include this pure model file when needed.

If extracting the model is too risky:
- include SmartXManagedOverrideManager.swift in the smoke command only if it compiles standalone
- if it does not compile standalone due to Logger/Paths/Settings dependencies, do not force it
- prefer model extraction

For ProfileArtifactManager smoke:
- If ProfileArtifactManager.swift references Paths, Logger, SmartXRedactor, or other app-only symbols in a way that breaks standalone compile, extract ProfileArtifactMetadata into a pure model file:
  ClashX/Models/ProfileArtifactMetadata.swift
  or similar
- Update manager and harness to use the shared model.
- Do not copy metadata structs into harness.

For diagnostics formatter/log reader/redactor:
- Ensure all needed production files are included.
- If the production file imports AppKit unnecessarily, remove AppKit import if Foundation is enough.
- Keep pure utility files standalone-compilable where practical.

For config validators:
- Ensure ConfigValidationIssue.swift is included.
- Avoid requiring full ClashConfig for standalone tests unless needed.
- Prefer validation input structs for harnesses.

Update BOTH:
- .github/workflows/pr-ci.yml
- scripts/codex-test-focused.sh

The local focused script and GitHub workflow must run the same smoke harness inventory, or the difference must be documented in Tests/README.md.

============================================================
PHASE 3 — Harden CI workflow against runner/toolchain instability
============================================================

Inspect .github/workflows/pr-ci.yml.

Current risks to check:

- macos-26 / Xcode 26.3 may be unavailable or renamed.
- validation-gate runs macos-15 / Xcode 16.4.
- modern artifact lane runs macos-26 / Xcode 26.3.
- upload-artifact if-no-files-found: error may hide earlier failure context if packaging did not run.
- repeated smoke harness commands exist in both validation gate and artifact lane.
- install_dependency.sh may run expensive network operations and fail transiently.
- workspace repair logic may be inconsistent between local scripts and CI.

Required fixes:

1. Make validation-gate the source of truth for smoke harnesses.
   The artifact lane should not duplicate every smoke harness unless there is a specific reason.
   Prefer:
   - validation-gate runs all smoke harnesses and helper checks
   - artifact lane depends on validation-gate and only builds/packages
   If you keep duplicate smoke checks in artifact lane, document why.

2. Add a workflow step that prints available Xcodes before selecting one:

ls -1 /Applications | grep -E '^Xcode' || true
xcodebuild -version || true

3. For the modern lane:
   If xcode-version "26.3" is brittle, use the nearest available 26.x selector or document how to update it.
   Do not remove the modern lane just to make CI pass.
   Do not reintroduce macOS 10.14 support.

4. Make artifact upload robust:
   - upload .ci-logs on failure
   - upload build logs even if app packaging fails
   - packaging step should fail clearly if SmartX.app is missing
   - logs should still be uploaded

5. Ensure workflow names make clear:
   - artifacts are unsigned
   - not notarized
   - manual testing only

6. If install_dependency.sh downloads resources, ensure logs are grouped and failure output is preserved.

7. Do not hide failures with `|| true` except for diagnostic commands.
   For actual validation commands, failures should fail the job.

============================================================
PHASE 4 — Fix scripts so local and CI behavior match
============================================================

Inspect and update:

scripts/codex-test-focused.sh
scripts/codex-lint-scripts.sh
scripts/codex-diagnose-project.sh
scripts/codex-build-debug.sh
scripts/ensure-xcworkspace.sh

Requirements:

1. scripts/codex-test-focused.sh should:
   - run the same smoke harnesses as validation-gate
   - compile each harness with the correct production dependency files
   - fail on real smoke harness failure
   - write full logs to .codex-logs
   - print concise failure summaries
   - continue? For a single test script, it may fail at the end, but internally it should attempt all smoke harnesses and report all failures instead of stopping at the first one.

2. Change the script from “stop after first harness failure” to “run all harnesses, collect failures, then exit nonzero if any failed.”

3. Add helper functions:
   run_swift_script
   run_swiftc_harness
   record_failure

4. Each harness result should be logged:
   PASS / FAIL / SKIP with reason.

5. If Xcode test target does not exist or workspace is unavailable:
   - report that separately
   - do not prevent smoke harnesses from running
   - exit nonzero only if required smoke harnesses fail
   - if xcodebuild test is not available in this repo yet, document it as skipped, not as a mysterious failure

6. scripts/codex-diagnose-project.sh should report:
   - current branch
   - workspace existence
   - Xcode version
   - available Xcodes if macOS
   - Go/Ruby/Bundler versions
   - selected workflow files
   - smoke harness inventory
   - whether PR head seems aligned if gh is available

7. scripts/codex-build-debug.sh should not perform signing.
   It should preserve build logs.
   It should print expected app path and fail clearly if app is missing.

============================================================
PHASE 5 — Fix project file inclusion for newly added source files
============================================================

Inspect:

ClashX.xcodeproj/project.pbxproj

Ensure every newly added production Swift file is included in the app target sources:

- ConfigAPI.swift
- ConnectionAPI.swift
- DiagnosticsAPI.swift
- ProviderAPI.swift
- SmartAPI.swift
- EffectiveConfigGenerator.swift
- DiagnosticsMaintenanceCoordinator.swift
- DiagnosticsProviderFormatter.swift
- any extracted model files such as SmartXManagedOverrideModel.swift or ProfileArtifactMetadata.swift

If a file is used only by tests/harnesses and not by app, document that.

Do not rely on a file existing in the repository but not being part of the Xcode target.

If modifying project.pbxproj manually:
- keep UUID style consistent
- do not remove existing source entries
- do not touch unrelated build settings

============================================================
PHASE 6 — Documentation and PR clarity
============================================================

Update:

Tests/README.md
docs/memory/13-test-strategy-and-legacy-hardening.md
docs/memory/14-review-findings-and-patchwork-risks.md
docs/memory/10-roadmap.md
docs/plan/smartx-mihomo-smart-update-plan.md if needed

Required documentation updates:

1. State that smoke harnesses are a transitional safety net, not high coverage.
2. List each smoke harness and whether it calls production code directly.
3. Document any harness that is skipped and why.
4. State that the effective config generator is currently unsupported/stub unless truly implemented.
5. State that ApiRequest split is partial.
6. State that Diagnostics Dashboard decomposition is partial.
7. State that CI artifacts are unsigned and not notarized.
8. State that 10.14 support is not part of current modern-only CI if that is still the project decision.

============================================================
PHASE 7 — Final verification pass
============================================================

Run these commands and collect results. Continue after failures.

git status --short
git diff --stat smartx...HEAD || git diff --stat
git diff --name-only smartx...HEAD || git diff --name-only

Branch/PR info if gh exists:

gh pr list --state open --json number,title,headRefName,baseRefName,isDraft,statusCheckRollup || true
gh pr view 11 --json number,title,headRefName,baseRefName,isDraft,statusCheckRollup || true

Text checks:

grep -R "Use ClashX LightGBM Settings" ClashX docs || true
grep -R "Generated Effective Config" ClashX docs || true
grep -R "ConfigManager.apiUrl + " ClashX || true
grep -R "webSocketUrl.appending" ClashX || true

Run scripts:

bash scripts/codex-lint-scripts.sh
bash scripts/codex-test-focused.sh
bash scripts/codex-diagnose-project.sh

Try build:

bash scripts/codex-build-debug.sh

If available and useful:

xcodebuild -list -workspace ClashX.xcworkspace
xcodebuild -resolvePackageDependencies -workspace ClashX.xcworkspace -scheme ClashX

Do not claim green CI unless local verification and/or GitHub checks actually pass.

============================================================
FINAL RESPONSE REQUIREMENTS
============================================================

Your final response must include:

1. Branch name.
2. Whether PR #11 head branch is aligned with the actual hardening branch.
3. CI Failure Ledger:
   - failed check
   - likely cause
   - fix applied
   - verification result
4. Harness Dependency Ledger:
   - harness name
   - production files compiled
   - PASS/FAIL/SKIP
   - reason if not PASS
5. Files changed, grouped by:
   - workflow
   - scripts
   - production pure-model extraction
   - project file
   - docs
6. Verification commands and results.
7. Known limitations.
8. Suggested PR title.
9. Suggested PR body.

Do not say “CI fixed” unless the relevant checks actually pass or the branch is pushed and GitHub checks pass.

If CI remains failing:
- identify the remaining failing check
- explain whether it is a real product issue, harness issue, runner/toolchain issue, or workspace issue
- give the next exact command or file to inspect