Revise docs/plan/smartx-mihomo-smart-update-plan.md against current smartx.

Keep the Smart-first product principle.

Update the current-codebase alignment to reflect that these are already present as first-pass groundwork:
- ControllerEndpointBuilder
- CoreCapabilityProbe
- SmartXRedactor
- SmartXManagedOverrideManager
- LightGBMSettingsViewModel
- TunLifecycleCoordinator
- TunConfigValidator / DNSConfigValidator
- source-copy profile artifacts

Do not describe these as future introduction work.

Rewrite the future PR plan around hardening and decomposition:
1. Documentation/memory sync
2. ApiRequest domain split
3. Diagnostics dashboard decomposition
4. Generated effective config MVP
5. TUN lifecycle hardening
6. Capability probe expansion
7. Real test target and harness promotion

Remove or qualify references to files that do not exist, such as post-merge-dmg.yml, unless explicitly marked as future release workflow.

Move uncertain endpoints such as group delay out of baseline mihomo capability unless verified.

Make clear that current managed override is groundwork, not full profile workspace.
Make clear that current TUN lifecycle is guarded external-controller patching, not embedded-core TUN support.
Make clear that current diagnostics dashboard is transitional and must be decomposed before adding more panels.