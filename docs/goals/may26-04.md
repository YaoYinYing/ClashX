You are working in `YaoYinYing/SmartX` on branch `codex/foundation-hardening-may26`.

This branch is broadly aligned with the intended foundation-hardening PR. It already does several correct things:

* Adds domain API clients such as `ConfigAPI`, `DiagnosticsAPI`, `ProviderAPI`, and `SmartAPI`.
* Shrinks `ApiRequest` and turns it into more of a compatibility facade.
* Adds `EffectiveConfigGenerator` as a safe interface rather than doing fragile YAML regex mutation.
* Extends `CoreCapabilityProbe` with non-mutating Smart weights probing.
* Keeps mutating endpoints such as Smart cache flush, LightGBM upgrade, config patch, restart, and GEO/UI update out of automatic probes.
* Adds diagnostics/provider formatting and updates tests/docs.

However, do not treat the branch as complete yet. Continue and finish the branch under the following strict closeout requirements.

## 1. Confirm scope and do not expand it

Do not add new features. Do not build the config editor. Do not implement full Merge/Script profile pipeline. Do not implement embedded-core TUN support. Do not introduce a user-facing multi-core selector. Do not rename bundle/helper identity.

This PR is a foundation-hardening PR only.

## 2. Re-check the seven original targets and mark each as complete, partial, or deferred

Create a short internal checklist while working. The seven target groups are:

1. Plan document revision
2. Api/domain split
3. Capability probing
4. Diagnostics dashboard decomposition
5. Effective config / artifact semantics
6. TUN lifecycle hardening
7. Tests and memory docs

For each group, either finish the scoped work or explicitly document why it remains partial.

## 3. Tighten the plan document against actual code

Inspect and update:

* `docs/plan/smartx-mihomo-smart-update-plan.md`
* `docs/memory/10-roadmap.md`
* `docs/memory/14-review-findings-and-patchwork-risks.md`

The plan must say that these are already first-pass groundwork, not future introductions:

* `ControllerEndpointBuilder`
* `CoreCapability` / `CoreCapabilityProbe`
* `SmartXRedactor`
* `SmartXManagedOverrideManager`
* `LightGBMSettingsViewModel`
* `TunLifecycleCoordinator`
* `TunConfigValidator` / `DNSConfigValidator`
* `DiagnosticsLogReader`
* `DiagnosticsArtifactFormatter`
* `DiagnosticsProviderFormatter`
* `ProfileArtifactManager`
* smoke harnesses under `Tests/SecurityHarness`

Do not overclaim. Use precise wording:

* `EffectiveConfigGenerator` currently does not generate a real effective YAML config if it only returns unsupported.
* Current profile artifacts remain source-copy or successful-reload artifacts unless a real generator writes a transformed config.
* Current TUN lifecycle remains guarded external-controller config patching and verification; embedded-core TUN is still unsupported.
* Diagnostics dashboard remains transitional unless it has been substantially decomposed.
* `ApiRequest` is partially split, not fully decomposed.

## 4. Validate the API split

Inspect:

* `ClashX/General/ApiRequest.swift`
* `ClashX/General/Api/ConfigAPI.swift`
* `ClashX/General/Api/DiagnosticsAPI.swift`
* `ClashX/General/Api/ProviderAPI.swift`
* `ClashX/General/Api/SmartAPI.swift`

Requirements:

* `ApiRequest` may remain as facade compatibility, but moved methods should delegate to domain API clients.
* All new/touched endpoint construction must use `ControllerEndpointBuilder`.
* No new direct `ConfigManager.apiUrl + "/path"` should be introduced.
* No new direct `ConfigManager.webSocketUrl.appending(...)` should be introduced.
* `DiagnosticsAPI` must not contain Smart-only endpoints.
* `SmartAPI` must not contain baseline diagnostics endpoints.
* Mutating endpoints must continue to return structured result states.

If the split is partial, document what remains in `ApiRequest` and why.

## 5. Validate capability probing

Inspect:

* `ClashX/General/Managers/CoreCapability.swift`
* `ClashX/General/Managers/CoreCapabilityProbe.swift`

Requirements:

* Do not use mutating endpoints as probes.
* Smart weights probing is acceptable if it is read-only.
* Smart cache flush, Smart connection block, LightGBM upgrade, config patch, restart, GEO update, UI update, and debug GC must not be auto-probed by performing the action.
* Missing Smart endpoints should result in `unsupported`, `unknown`, or `degraded`, not app failure.
* Plain mihomo-compatible runtime should continue to support baseline functions with Smart features disabled.
* Capability identity must not contain raw secrets.

## 6. Validate `EffectiveConfigGenerator`

Inspect:

* `ClashX/General/Managers/EffectiveConfigGenerator.swift`
* `ClashX/General/Managers/ProfileArtifactManager.swift`
* `ClashX/General/Utils/DiagnosticsReportBuilder.swift`
* `Tests/SecurityHarness/effective_config_generator_smoke.swift`

Requirements:

* If no safe YAML parse/emit path exists, keep `EffectiveConfigGenerator` as an explicit unsupported interface.
* Do not implement generated config through brittle regex or broad text replacement.
* Do not mutate remote subscription YAML.
* Do not call current source-copy artifacts “Generated Effective Config” in user-facing UI/report text.
* If “Generated Effective Config” appears in docs, it must refer to future roadmap or compatibility alias only.
* If generated config is not implemented, docs and test names must say so honestly.

## 7. Validate diagnostics dashboard reduction

Inspect:

* `ClashX/ViewControllers/Connections/DiagnosticsDashboardViewController.swift`
* `ClashX/General/Utils/DiagnosticsLogReader.swift`
* `ClashX/General/Utils/DiagnosticsArtifactFormatter.swift`
* `ClashX/General/Utils/DiagnosticsProviderFormatter.swift`
* `ClashX/General/Utils/DiagnosticsReportBuilder.swift`
* `ClashX/General/Utils/DiagnosticsBundleExporter.swift`

Requirements:

* Large log files must not be fully read every two seconds.
* Provider formatting should use `DiagnosticsProviderFormatter` if available.
* Artifact formatting should use `DiagnosticsArtifactFormatter` if available.
* Dangerous actions must still require confirmation:

  * restart core
  * update dashboard
  * update GEO assets
  * run debug GC
  * restore last-known-good
* Reports and bundles must pass through `SmartXRedactor`.
* `DiagnosticsDashboardViewController` should not grow new responsibilities in this branch.

If dashboard decomposition is still minimal, document it as partial.

## 8. Validate TUN lifecycle

Inspect:

* `ClashX/General/Managers/TunLifecycleCoordinator.swift`
* `ClashX/General/Utils/TunConfigValidator.swift`
* `ClashX/General/Utils/DNSConfigValidator.swift`
* `ClashX/ViewControllers/Settings/CoreSettingViewController.swift`

Requirements:

* Embedded-core TUN must remain explicitly unsupported unless a real architecture exists.
* Core Settings must use `TunLifecycleCoordinator` and not bypass it.
* Enabling TUN must run validators and distinguish warning vs blocking issues.
* UI messaging should distinguish:

  * success
  * requested but unverified
  * unsupported
  * unauthorized
  * blocked by validation
  * failed
* Do not add root/helper hacks.

If this branch did not touch TUN lifecycle beyond existing code, document it as unchanged and already first-pass only.

## 9. Strengthen tests and scripts

Inspect:

* `Tests/README.md`
* `Tests/SecurityHarness/`
* `scripts/codex-test-focused.sh`
* `.github/workflows/pr-ci.yml`

Requirements:

* Ensure any new smoke harness is listed in `scripts/codex-test-focused.sh`.
* Ensure `Tests/README.md` documents which harnesses call production code and which limitations remain.
* Add or update a smoke harness if you added new pure logic.
* Do not copy production logic into fake tests.

At minimum, verify harness coverage for:

* effective config generator unsupported behavior
* endpoint builder
* redactor
* config validators
* managed override persistence if already present
* profile artifact metadata if already present

## 10. Run strict verification and do not stop at first failure

Run the following commands and collect results. If one fails, continue to the next.

```bash
git status --short
git diff --stat smartx...HEAD || git diff --stat
git diff --name-only smartx...HEAD || git diff --name-only

grep -R "Use ClashX LightGBM Settings" ClashX docs || true
grep -R "Generated Effective Config" ClashX docs || true
grep -R "ConfigManager.apiUrl + " ClashX || true
grep -R "webSocketUrl.appending" ClashX || true

bash scripts/codex-lint-scripts.sh
bash scripts/codex-test-focused.sh
bash scripts/codex-diagnose-project.sh
bash scripts/codex-build-debug.sh
```

If `codex-build-debug.sh` fails because of local Xcode/macOS/dependency limitations, record that honestly. Do not claim build success unless it succeeds.

## 11. Final response requirements

Your final response must include:

* Branch name
* Summary of changes
* A seven-row Work Ledger:

  1. Plan document
  2. Api/domain split
  3. Capability probing
  4. Diagnostics dashboard
  5. Effective config / artifact semantics
  6. TUN lifecycle
  7. Tests/docs
* Verification results table
* Known limitations
* Suggested PR title
* Suggested PR description

Do not claim:

* full generated effective config
* full TUN support
* high test coverage
* release readiness
* signing or notarization readiness

Suggested PR title:

`Harden SmartX foundation: API domains, capability probing, diagnostics, and config semantics`
