You are working in the YaoYinYing/SmartX repository.

Your task is to create ONE consolidated PR that hardens the current SmartX foundation. This is a large bounded PR. It must not become a random feature blob.

Start from the latest branch that contains the planning document if it exists:

- Prefer branch: codex/plan-doc-may26
- If that branch does not exist locally, start from smartx and recreate or update the plan document as needed.

Create or continue a working branch named:

codex/foundation-hardening-may26

Core situation:

The codebase has already moved forward. Do NOT assume these are missing:
- ControllerEndpointBuilder already exists.
- CoreCapability and CoreCapabilityProbe already exist.
- SmartXRedactor already exists.
- SmartXManagedOverrideManager already exists.
- LightGBMSettingsViewModel already exists.
- TunLifecycleCoordinator already exists.
- TunConfigValidator and DNSConfigValidator already exist.
- DiagnosticsLogReader / DiagnosticsArtifactFormatter / DiagnosticsBundleExporter may already exist.
- The diagnostics dashboard, Smart dashboard, Core settings page, profile artifacts, smoke harnesses, and memory docs already exist in first-pass form.

Therefore this PR is NOT about introducing those concepts from scratch. It is about hardening, correcting, decomposing, and aligning them.

Primary objective:

Turn the current SmartX foundation from “first-pass patchwork” into a safer, more coherent baseline.

This PR must handle exactly these seven problem groups:

1. Revise the plan document against the real current codebase.
2. Decompose ApiRequest enough to stop it from being the permanent endpoint dumping ground.
3. Harden capability probing and capability cache semantics without adding mutating probes.
4. Further decompose DiagnosticsDashboardViewController and reduce its god-controller risk.
5. Prepare a minimal generated effective config MVP only for LightGBM managed override, if feasible without destabilizing the branch.
6. Harden TUN lifecycle and validation semantics without pretending embedded-core TUN is supported.
7. Strengthen tests/smoke harnesses and memory docs so this PR leaves evidence, not promises.

Non-goals:

Do NOT build a full config editor.
Do NOT build a full profile workspace UI.
Do NOT implement Merge/Script profile pipeline.
Do NOT implement full embedded-core TUN support.
Do NOT add user-facing multi-core selection.
Do NOT rename the whole app bundle/helper identity.
Do NOT perform Developer ID signing/notarization work.
Do NOT rewrite AppDelegate wholesale.
Do NOT remove all legacy ClashX paths or class names.
Do NOT add another large view controller.
Do NOT claim “generated effective config” unless a file is actually generated from base config plus managed override.
Do NOT mutate remote subscription YAML.
Do NOT add remote CDN editor assets.
Do NOT introduce SwiftUI.
Do NOT introduce Swift concurrency as a required runtime path.
Do NOT break macOS 10.14/10.15 compatibility without availability guards and explicit notes.

Execution discipline:

This task must not stop early just because one command fails, one file is missing, or one subtask is too large.

You must keep a running Work Ledger in your final response with:
- each target group
- files inspected
- files changed
- verification performed
- remaining limitations

If a subtask is blocked, do not abandon the whole task. Record the blocker, complete the next best scoped improvement, and continue through the remaining targets.

Do not ask for clarification. Make the safest maintainable assumption and document it.

Before editing, run or inspect:

git status --short
git branch --show-current
git log --oneline --decorate -5
git diff --stat smartx...HEAD || true
find docs -maxdepth 3 -type f | sort | sed -n '1,160p'
find ClashX/General -maxdepth 3 -type f | sort | sed -n '1,220p'
find Tests -maxdepth 3 -type f | sort

Inspect these files if present:

AGENTS.md
README.md
docs/plan/smartx-mihomo-smart-update-plan.md
docs/memory/10-roadmap.md
docs/memory/14-review-findings-and-patchwork-risks.md
docs/memory/13-test-strategy-and-legacy-hardening.md
ClashX/General/ApiRequest.swift
ClashX/General/Utils/ControllerEndpointBuilder.swift
ClashX/General/Managers/CoreCapability.swift
ClashX/General/Managers/CoreCapabilityProbe.swift
ClashX/General/Managers/SmartXManagedOverrideManager.swift
ClashX/General/Managers/LightGBMSettingsViewModel.swift
ClashX/General/Managers/ProfileArtifactManager.swift
ClashX/General/Managers/TunLifecycleCoordinator.swift
ClashX/General/Utils/TunConfigValidator.swift
ClashX/General/Utils/DNSConfigValidator.swift
ClashX/General/Utils/DiagnosticsLogReader.swift
ClashX/General/Utils/DiagnosticsArtifactFormatter.swift
ClashX/General/Utils/DiagnosticsReportBuilder.swift
ClashX/General/Utils/DiagnosticsBundleExporter.swift
ClashX/ViewControllers/Connections/DiagnosticsDashboardViewController.swift
ClashX/ViewControllers/Connections/SmartDashboardViewController.swift
ClashX/ViewControllers/Settings/CoreSettingViewController.swift
ClashX/Models/ClashConfig.swift
ClashX/Macro/Paths.swift
Tests/README.md
Tests/SecurityHarness/

After inspection, write a short local plan in your notes. Then implement the seven target groups below.

============================================================
TARGET 1 — Revise the plan document against the real codebase
============================================================

Problem:
The plan document may still describe some already-existing groundwork as future work. It must now reflect the current state.

Update:

docs/plan/smartx-mihomo-smart-update-plan.md

If the file does not exist, create it.

Requirements:

1. Keep the strategic principle:
   SmartX is a Smart-first macOS mihomo-compatible client.
   Users should not see a core marketplace or ordinary multi-core selector.
   Product layer: one normal bundled Smart runtime.
   Architecture layer: maintainer-visible core migration boundaries.

2. Update “Current Codebase Alignment” to say that these are already present as first-pass groundwork, not future introductions:
   - ControllerEndpointBuilder
   - CoreCapability / CoreCapabilityProbe
   - SmartXRedactor
   - SmartXManagedOverrideManager
   - LightGBMSettingsViewModel
   - TunLifecycleCoordinator
   - TunConfigValidator / DNSConfigValidator
   - ProfileArtifactManager with source-copy semantics
   - DiagnosticsDashboardViewController as a transitional dashboard
   - smoke harnesses under Tests/SecurityHarness

3. Be honest:
   - SmartXManagedOverrideManager is groundwork, not a full profile workspace.
   - Profile artifacts are source-copy or successful-reload artifacts unless a real generator exists.
   - TUN lifecycle is initial guarded external-controller flow, not embedded-core TUN support.
   - DiagnosticsDashboardViewController is transitional and still too large.
   - ApiRequest is still too broad.
   - Testing is improving but not high coverage.

4. Remove or qualify references to nonexistent files. If a workflow such as post-merge-dmg.yml exists, keep it. If not, call it a future release workflow.

5. Move uncertain endpoints, such as group-delay style endpoints, out of baseline mihomo capability unless source code and docs prove they are baseline. Mark them guarded or runtime-probed.

6. Rewrite the future PR plan around hardening and decomposition, not first-time introduction.

Preferred future PR sequence in the document:
   1. ApiRequest domain split
   2. Diagnostics dashboard decomposition
   3. Generated effective config MVP
   4. TUN lifecycle hardening
   5. Capability probe expansion
   6. Real test target / smoke harness promotion
   7. Config Workspace MVP
   8. Release/signing/supply chain work

Do not overclaim.

============================================================
TARGET 2 — Decompose ApiRequest enough to create a real boundary
============================================================

Problem:
ApiRequest is still too large and mixes baseline mihomo endpoints, Smart-only endpoints, diagnostics, maintenance, providers, connections, config patching, and streams.

This PR should not rewrite all callers. It should introduce domain clients and move a useful first slice.

Create new files as needed, for example:

ClashX/General/Api/ControllerAPIResult.swift
ClashX/General/Api/ConfigAPI.swift
ClashX/General/Api/ProviderAPI.swift
ClashX/General/Api/DiagnosticsAPI.swift
ClashX/General/Api/SmartAPI.swift
ClashX/General/Api/ConnectionAPI.swift

If project organization prefers another folder, use it, but keep names clear.

Rules:

1. Keep ApiRequest as a compatibility facade for now.
2. Move implementation of at least these groups into domain clients:
   - DiagnosticsAPI:
     memory snapshot
     DNS query
     DNS cache flush
     fake-ip flush
     restart
     debug GC
     pprof URL helper if suitable
     GEO reload/update
     dashboard update
   - ProviderAPI:
     proxy providers diagnostics
     rule providers diagnostics
     provider update / health check if present
   - SmartAPI:
     smart weights
     smart cache flush
     smart connection block
     LightGBM upgrade
   - ConfigAPI:
     request controller config
     patch config
     update TUN result if feasible
3. ApiRequest methods may remain but should delegate to these clients.
4. All moved endpoint code must use ControllerEndpointBuilder.
5. No new direct string concatenation with ConfigManager.apiUrl is allowed in touched code.
6. Do not change external behavior unless required to fix a bug.
7. Preserve existing result semantics:
   success
   unsupported
   unauthorized
   failed/degraded

Add comments explaining:
- baseline mihomo client
- Smart extension client
- diagnostics/maintenance client

Acceptance checks:
- grep touched code for ConfigManager.apiUrl + and webSocketUrl.appending.
- ApiRequest should be smaller or at least have clear delegating sections.
- New domain clients must not themselves become giant dumping grounds.

If full split is too large:
Move at least DiagnosticsAPI and SmartAPI implementation. Leave the rest delegated or documented.

============================================================
TARGET 3 — Harden capability probing and cache semantics
============================================================

Problem:
CoreCapabilityProbe exists, but it is still minimal. It should become more accurate without using mutating endpoints as probes.

Modify CoreCapability, CoreCapabilityProbe, and CapabilityCache as needed.

Requirements:

1. Do not probe mutating endpoints by mutating user state.
   Do NOT call cache flush, restart, update GEO, update UI, Smart cache flush, or LightGBM upgrade as probes.

2. Add safe Smart capability probing if feasible:
   - Smart weights endpoint can be probed if it is read-only.
   - Smart group weight endpoint can be optional.
   - If a Smart endpoint requires a configured Smart group and none exists, mark degraded or unsupported with a clear message.

3. Add better snapshot metadata:
   - controller identity
   - mode embedded/external
   - running/stopped
   - core version if known
   - probedAt
   - maybe app-visible core metadata if easy

4. Do not include raw secrets in identity.
   Existing hashed or no-secret/secret-set behavior is acceptable.

5. CapabilityCache should remain thread-safe.
   Do not regress its serial queue.

6. Make capability messages useful for DiagnosticsReportBuilder.

7. If Smart probing is not safe or not enough information is available, explicitly mark Smart capabilities unknown or degraded, not available.

8. Update DiagnosticsDashboardViewController and SmartDashboardViewController to consume the improved capability state where low-risk.

Acceptance checks:
- No mutating endpoints are used in CoreCapabilityProbe.
- Capability report distinguishes unknown vs unsupported vs unavailable vs degraded.
- Smart Dashboard does not fail if Smart endpoint is absent.
- Ordinary mihomo-compatible baseline should still work with Smart controls disabled.

============================================================
TARGET 4 — Decompose DiagnosticsDashboardViewController further
============================================================

Problem:
DiagnosticsDashboardViewController is still too large. It should not keep absorbing log reading, artifact formatting, provider formatting, maintenance actions, and export behavior.

This PR should reduce complexity without a full UI rewrite.

Requirements:

1. If DiagnosticsLogReader already exists, ensure DiagnosticsDashboardViewController uses it consistently.
   If it does not exist, create it.
   It must avoid reading entire large log files every two seconds.
   Use a simple tail strategy:
   - read only last 1–2 MB for large files
   - filter after tailing
   - show last 200 matching lines

2. If DiagnosticsArtifactFormatter already exists, ensure artifact formatting lives there.
   If it does not exist, create it.

3. Extract at least one additional helper:
   - DiagnosticsProviderFormatter or DiagnosticsProviderViewModel
   - DiagnosticsMaintenanceCoordinator
   - DiagnosticsReportActionCoordinator
   Choose the highest value with lowest risk.

4. Keep dangerous actions confirmed:
   - restart core
   - update dashboard
   - update GEO assets
   - run debug GC
   - restore last good

5. Do not add new diagnostics features.
6. Do not add more buttons.
7. Do not make DiagnosticsDashboardViewController larger unless extracted files reduce net responsibility.

8. Ensure it conforms to DashboardSubViewControllerProtocol.
   actionSearch(string:) should update logSearchField or behave as a documented no-op.

Acceptance checks:
- DiagnosticsDashboardViewController line count or responsibility should decrease.
- Log reading does not synchronously read full multi-MB files on every timer tick.
- Dangerous actions still require confirmation.
- Exported reports/bundles still use SmartXRedactor.

============================================================
TARGET 5 — Minimal generated effective config MVP for LightGBM override
============================================================

Problem:
SmartXManagedOverrideManager exists, but ProfileArtifactManager still stores source-copy artifacts. SmartX needs a tiny effective-config generator to prove the concept, but only for LightGBM managed override.

This target is conditional:
Implement it only if it can be done safely without large YAML tooling or brittle text surgery. If not feasible, implement a documented stub and tests around the current limitation.

Preferred implementation:

1. Create a small generator, for example:
   ClashX/General/Managers/EffectiveConfigGenerator.swift

2. Scope:
   base config + SmartX managed LightGBM override -> generated effective config

3. Do not implement full Merge/Script profile pipeline.
4. Do not mutate remote subscription files.
5. Do not transform arbitrary unrelated config fields.

6. If current dependencies provide YAML parse/emit safely, use them.
   If not, do NOT do fragile broad regex rewriting of YAML.
   In that case:
   - create the generator interface
   - return a structured “unsupported until YAML emitter exists” result
   - update docs explaining why generated effective config is not complete
   - keep source-copy artifact semantics

7. If generator is implemented:
   - write generated-effective config to the existing artifact path or a clearly named path
   - metadata must set:
     generationMode: "smartx-lightgbm-override"
     includesSmartXOverrides: true
     includesProfileMerge: false
     includesRuntimeOverrides: false or true only if truly represented
   - validate generated config using existing verifyClashConfig if feasible
   - on failure, do not overwrite last-known-good
   - preserve source-copy artifact separately or metadata source path

8. If generator is not implemented:
   - rename current behavior clearly as Successful Reload Artifact / Loaded Source Copy
   - do not call it generated effective config in current UI/report
   - docs must say generator is pending due to safe YAML emit requirement

Acceptance checks:
- No remote subscription YAML is mutated.
- No fragile full-file regex config rewrite is introduced.
- Metadata is honest.
- If generated effective config is produced, it actually includes LightGBM override fields.
- If not produced, UI/docs do not claim it exists.

============================================================
TARGET 6 — Harden TUN lifecycle and validation semantics
============================================================

Problem:
TunLifecycleCoordinator and validators exist. This PR should harden them without pretending embedded TUN works.

Requirements:

1. Keep embedded core TUN explicitly unsupported unless real architecture exists.
2. Improve lifecycle state messages:
   - preflight
   - blocked by validation
   - requested
   - verified success
   - requested but unverified
   - failed
   - unsupported
   - unauthorized

3. Ensure CoreSettingViewController displays these states clearly.

4. Add or improve recovery text:
   - when blocked by validation
   - when update requested but not verified
   - when controller reports mismatched final state
   - when embedded core is selected

5. Validators:
   - TunConfigValidator should keep CIDR validation strict enough for IPv4/IPv6.
   - DNSConfigValidator should not overclaim full DNS validation.
   - Severity names should be clear: info, warning, blocking.

6. If enabling TUN, run both TUN and DNS validators and distinguish blocking vs warning.
   Warnings should not always block.
   Blocking errors should block.

7. No root/helper hacks.
8. No new TUN editor.
9. No claim of full TUN runtime support.

Acceptance checks:
- Embedded core TUN remains blocked with clear explanation.
- External controller TUN update goes through TunLifecycleCoordinator.
- Core settings no longer bypasses the coordinator.
- Validation issues are reusable and testable.

============================================================
TARGET 7 — Tests, smoke harnesses, and memory docs
============================================================

Problem:
The repo has smoke harnesses but still lacks high coverage. This PR must leave better verification, not just more code.

Requirements:

1. Add or update smoke harnesses for any new pure logic:
   - Api domain clients if feasible
   - CoreCapabilityProbe safe Smart probing if feasible
   - EffectiveConfigGenerator or its unsupported result
   - TunLifecycleCoordinator state transitions if feasible
   - Diagnostics helper extraction
   - SmartXManagedOverrideManager if changed

2. Do not copy production logic into harnesses.
   Harnesses should call production code where practical.
   If import/build structure blocks that, document the limitation.

3. Update Tests/README.md:
   - what harnesses exist
   - which call production code
   - which are temporary
   - how to run focused checks
   - what still needs real XCTest target

4. Update memory docs:
   - docs/memory/10-roadmap.md
   - docs/memory/14-review-findings-and-patchwork-risks.md
   - docs/memory/13-test-strategy-and-legacy-hardening.md
   - docs/memory/04-mihomo-api-and-capabilities.md
   - docs/memory/05-tun-and-dns-roadmap.md
   - docs/memory/06-profile-and-config-enhancement.md
   - docs/memory/07-smart-core-and-lightgbm.md
   - docs/memory/08-diagnostics-and-observability.md

5. Memory docs must match implementation:
   - If generator is not implemented, say it is not implemented.
   - If ApiRequest is only partially split, say partial.
   - If TUN lifecycle is still external-controller only, say so.
   - If diagnostics dashboard remains transitional, say so.

Acceptance checks:
- docs do not overclaim.
- Tests/README reflects current reality.
- scripts/codex-test-focused.sh is updated if new harnesses are added.

============================================================
STRICT VERIFICATION PROTOCOL
============================================================

After implementation, run the following checks. Do not stop after the first failure. Continue, collect output, and summarize.

A. Git and diff checks:

git status --short
git diff --stat smartx...HEAD || git diff --stat
git diff --name-only smartx...HEAD || git diff --name-only

B. Forbidden wording / overclaim checks:

grep -R "Use ClashX LightGBM Settings" ClashX docs || true
grep -R "Generated Effective Config" ClashX docs || true
grep -R "core marketplace" README.md docs ClashX || true
grep -R "universal core switcher" README.md docs ClashX || true

If "Generated Effective Config" remains, it must only appear in future-roadmap or compatibility-comment context. It must not describe current source-copy artifacts unless a real generator exists.

C. Endpoint safety checks:

grep -R "ConfigManager.apiUrl + " ClashX || true
grep -R "webSocketUrl.appending" ClashX || true

No new touched code may add direct endpoint string concatenation.

D. Diagnostics redaction checks:

grep -R "remoteURL" ClashX/General/Utils/DiagnosticsReportBuilder.swift ClashX/General/Utils/DiagnosticsBundleExporter.swift || true
grep -R "sourceRemoteURL" ClashX/General/Utils/DiagnosticsReportBuilder.swift ClashX/General/Utils/DiagnosticsBundleExporter.swift || true

If these appear, verify they pass through SmartXRedactor before export.

E. Run focused scripts if present:

bash scripts/codex-lint-scripts.sh
bash scripts/codex-test-focused.sh
bash scripts/codex-diagnose-project.sh

F. Try a debug build if feasible:

bash scripts/codex-build-debug.sh

If the build fails because of unavailable macOS/Xcode environment, dependency availability, signing, or runner limitation, record that honestly. Do not claim success.

G. Optional manual source checks:

wc -l ClashX/ViewControllers/Connections/DiagnosticsDashboardViewController.swift
wc -l ClashX/General/ApiRequest.swift

These counts are not pass/fail by themselves, but the PR should not make both bigger without extraction.

============================================================
FAILURE HANDLING RULES
============================================================

This task must not terminate early unless the repository is unavailable.

If a file is missing:
- search for its equivalent
- adapt the task
- document the path difference

If a command fails:
- record the command and error summary
- continue to the next check
- do not throw away completed work

If generated effective config is unsafe to implement:
- do not hack it with regex
- create a safe interface or explicit unsupported result
- update docs to say why it is pending

If ApiRequest split is too large:
- move DiagnosticsAPI and SmartAPI first
- leave facade compatibility
- document remaining slices

If tests cannot import production code:
- add or update smoke harness only if it calls production code
- otherwise document the blocker in Tests/README.md
- do not copy logic into fake tests

If Xcode build is unavailable:
- still run script lint and focused tests
- document build not executed

============================================================
FINAL RESPONSE REQUIREMENTS
============================================================

Your final response must include:

1. Branch name.
2. Summary of implemented changes.
3. Work Ledger table with seven rows:
   - Plan doc
   - Api/domain split
   - Capability probing
   - Diagnostics dashboard
   - Effective config / artifact semantics
   - TUN lifecycle
   - Tests/docs
4. Verification results:
   - command
   - success/failure
   - short note
5. Known limitations.
6. Suggested PR title.
7. Suggested PR description.

Do not say “done” unless verification was attempted.
Do not claim high test coverage.
Do not claim full TUN support.
Do not claim full generated effective config unless implemented and verified.
Do not claim release readiness, signing readiness, or notarization readiness.