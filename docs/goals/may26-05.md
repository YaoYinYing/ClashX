You are working in the YaoYinYing/SmartX repository.

Create one bounded PR from the latest smartx branch.

Suggested branch name:

codex/tun-preflight-verification-boundary

Suggested PR title:

Harden TUN preflight and verification boundary before helper-backed runtime work

Context:

PR14 audited the privileged helper boundary.
PR15 added a typed helper command contract and kept future TUN commands reserved.
Now we need PR16: improve the current TUN lifecycle boundary without implementing helper-backed TUN.

Primary goal:

Make the current SmartX TUN path honest, typed, diagnosable, and recoverable.

SmartX currently has a guarded external-controller TUN update path through the controller API. This PR should clarify and harden that path.

It must distinguish:

- config-level TUN validation
- DNS validation
- controller capability
- embedded-core unsupported state
- helper status
- reserved helper TUN command contract
- controller-state verification after PATCH /configs
- system-level TUN verification, which is still NOT implemented

Hard non-goals:

- Do NOT implement helper-backed TUN.
- Do NOT implement embedded-core TUN.
- Do NOT create utun interfaces.
- Do NOT modify routes.
- Do NOT modify DNS settings at system level.
- Do NOT start mihomo through the helper.
- Do NOT add new helper XPC methods.
- Do NOT change ProxyConfigHelper.m.
- Do NOT change ProxyConfigRemoteProcessProtocol.h.
- Do NOT add generic privileged command execution.
- Do NOT call route, ifconfig, networksetup, launchctl, or shell commands for TUN.
- Do NOT add signing/notarization changes.
- Do NOT claim real system TUN verification.
- Do NOT build a full TUN settings UI.
- Do NOT change ordinary system proxy behavior.

Before editing, inspect:

- ClashX/General/Managers/TunLifecycleCoordinator.swift
- ClashX/General/Utils/TunConfigValidator.swift
- ClashX/General/Utils/DNSConfigValidator.swift
- ClashX/General/Utils/ConfigValidationIssue.swift
- ClashX/General/Managers/CoreCapability.swift
- ClashX/General/Managers/CoreCapabilityProbe.swift
- ClashX/General/Managers/HelperDiagnosticsProbe.swift
- ClashX/Models/HelperStatus.swift
- ClashX/Models/HelperCommandContract.swift
- ClashX/General/Utils/HelperCommandRegistry.swift
- ClashX/General/ApiRequest.swift
- ClashX/General/Api/ConfigAPI.swift
- ClashX/ViewControllers/Settings/CoreSettingViewController.swift
- ClashX/ViewControllers/Connections/DiagnosticsDashboardViewController.swift
- ClashX/General/Utils/DiagnosticsReportBuilder.swift
- Tests/SecurityHarness/config_validator_smoke.swift
- Tests/SecurityHarness/helper_command_contract_smoke.swift
- scripts/codex-test-focused.sh
- .github/workflows/pr-ci.yml
- docs/architecture/helper-boundary-audit.md
- docs/architecture/helper-command-contract.md
- docs/memory/05-tun-and-dns-roadmap.md
- docs/memory/10-roadmap.md
- docs/memory/13-test-strategy-and-legacy-hardening.md
- docs/memory/14-review-findings-and-patchwork-risks.md
- docs/plan/smartx-mihomo-smart-update-plan.md

Initial discovery:

git status --short
git branch --show-current
git log --oneline --decorate -8
grep -R "TunLifecycleCoordinator\|TunConfigValidator\|DNSConfigValidator\|updateTun\|tunPreflight\|tunEnable\|tunDisable\|helper-backed TUN\|embedded-core TUN" ClashX Tests docs scripts -n || true

Then complete the targets below.

============================================================
TARGET 1 — Add pure TUN preflight / verification models
============================================================

Add a Foundation-only model file.

Suggested path:

ClashX/Models/TunLifecycleDiagnostics.swift

Define typed concepts only. This file must not call controller API, helper API, shell, XPC, route, DNS, or system network commands.

Suggested types:

enum TunRuntimeMode: String, Codable {
    case externalController
    case embeddedCoreUnsupported
}

enum TunPreflightBlocker: String, Codable, CaseIterable {
    case controllerNotRunning
    case embeddedCoreUnsupported
    case controllerConfigUnavailable
    case tunSectionMissing
    case tunValidationFailed
    case dnsValidationFailed
    case configPatchUnsupported
    case controllerUnauthorized
    case helperTunReservedOnly
}

enum TunVerificationScope: String, Codable {
    case controllerConfigOnly
    case systemTunNotImplemented
}

enum TunVerificationOutcome: String, Codable {
    case notAttempted
    case requestedButUnverified
    case controllerStateMatches
    case controllerStateMismatch
    case failed
}

struct TunPreflightReport: Codable {
    var runtimeMode: TunRuntimeMode
    var canAttemptControllerPatch: Bool
    var blockers: [TunPreflightBlocker]
    var warnings: [String]
    var helperTrustState: HelperTrustState
    var helperTunCommandsReserved: Bool
    var verificationScope: TunVerificationScope
    var userMessage: String
    var recoverySuggestion: String
}

struct TunLifecycleVerificationReport: Codable {
    var expectedEnabled: Bool
    var outcome: TunVerificationOutcome
    var verificationScope: TunVerificationScope
    var controllerReportedEnabled: Bool?
    var message: String
    var recoverySuggestion: String
}

Rules:

1. Keep the model pure and Codable.
2. Do not depend on AppKit.
3. Do not depend on SwiftyJSON.
4. Do not depend on ConfigManager.
5. Do not claim system-level TUN verification.
6. Make helper-backed TUN clearly reserved-only.

Add the file to the app target in project.pbxproj.

============================================================
TARGET 2 — Extract preflight assembly from TunLifecycleCoordinator
============================================================

TunLifecycleCoordinator currently mixes preflight, validation, controller patching, and messaging.

Add a focused helper, for example:

ClashX/General/Managers/TunPreflightPlanner.swift

or:

ClashX/General/Utils/TunPreflightPlanner.swift

It may depend on existing app models and validators.

Suggested responsibility:

- accept current config snapshot, controller running state, embedded-core flag, capability cache status, helper status, and helper command contract
- return TunPreflightReport
- do not perform network requests
- do not patch config
- do not call helper XPC
- do not mutate state

Example method:

static func buildReport(
    config: ClashConfig?,
    isControllerRunning: Bool,
    isUsingEmbeddedCore: Bool,
    configPatchAvailability: CoreEndpointAvailability,
    helperStatus: HelperStatus,
    helperTunDescriptors: [HelperCommandDescriptor]
) -> TunPreflightReport

The report should say:

- embedded-core TUN is unsupported
- external-controller TUN can only be attempted through controller API
- helper-backed TUN commands are reserved only
- helper status does not imply TUN support
- system-level verification is not implemented

Use existing TunConfigValidator and DNSConfigValidator for config validation.

============================================================
TARGET 3 — Rewire TunLifecycleCoordinator around typed reports
============================================================

Update TunLifecycleCoordinator to use the new preflight report.

Required behavior:

1. If controller is not running:
   - return failed or unsupported with a message from TunPreflightReport
   - do not patch config

2. If embedded core is selected:
   - return unsupported
   - message must clearly say embedded-core TUN is unsupported
   - message must mention helper-backed TUN remains reserved/future

3. If config cannot be loaded:
   - return failed with controller config unavailable message

4. If tun section is missing:
   - return unsupported with tun section missing message

5. If enabling TUN and validators find blocking issues:
   - return blockedByValidation
   - include validation issues
   - do not patch config

6. If controller API patch is unsupported:
   - return unsupported
   - do not imply helper can take over

7. If controller patch succeeds:
   - verify controller config state only
   - report controllerStateMatches if tun.enable matches expected
   - report controllerStateMismatch if it differs
   - report requestedButUnverified if config reload/verification fails

8. Do not claim route, utun, packet, or DNS runtime verification.

9. Preserve the current external-controller TUN behavior as much as possible.

10. Do not change UI semantics beyond clearer messages.

============================================================
TARGET 4 — Add diagnostics output for TUN lifecycle boundary
============================================================

Update DiagnosticsReportBuilder.

Add a section such as:

TUN Lifecycle Boundary
----------------------

It should include:

- runtime mode
- whether external-controller patch can be attempted
- helper trust state
- whether helper TUN commands are reserved only
- verification scope
- current tun section present/missing
- current tun.enable if known
- warnings / blockers
- note that system-level TUN verification is not implemented

Rules:

1. No secrets.
2. No helper prompts.
3. No controller mutation.
4. No shell commands.
5. No route/DNS system probing.
6. It is okay if diagnostics uses the current ConfigManager snapshot only.

Optional:
Update DiagnosticsDashboardViewController only if low-risk, for example by adding the TUN boundary text into existing diagnostics output. Do not add new buttons.

============================================================
TARGET 5 — Tighten Core Settings TUN messaging without expanding UI
============================================================

Inspect CoreSettingViewController and related UI code.

If low-risk, update the TUN toggle explanatory text to distinguish:

- external-controller TUN patch path
- embedded-core unsupported path
- helper-backed TUN reserved/future path
- system-level verification not implemented

Rules:

1. Do not add a full TUN panel.
2. Do not add helper install buttons.
3. Do not add route/DNS checks.
4. Do not enable embedded-core TUN.
5. Do not allow helper status alone to enable TUN.
6. Do not change the setting if no safe existing UI hook exists; document as follow-up instead.

============================================================
TARGET 6 — Add smoke harness coverage
============================================================

Add:

Tests/SecurityHarness/tun_lifecycle_diagnostics_smoke.swift

It should compile production pure model files and planner if possible.

Test at least:

1. Embedded core produces embeddedCoreUnsupported blocker.
2. Controller not running blocks controller patch.
3. Missing tun section blocks or reports unsupported.
4. Helper TUN commands are reserved only.
5. Verification scope is controllerConfigOnly or systemTunNotImplemented, never full system verification.
6. Codable round-trip for TunPreflightReport.
7. Codable round-trip for TunLifecycleVerificationReport.
8. No report text claims helper-backed TUN is implemented.

If compiling TunPreflightPlanner standalone requires too many app dependencies, keep smoke harness focused on pure models and document planner coverage as build-level only.

Update:

- scripts/codex-test-focused.sh
- .github/workflows/pr-ci.yml
- Tests/README.md

Do not copy production structs into harnesses.

============================================================
TARGET 7 — Documentation sync
============================================================

Update:

- docs/architecture/helper-boundary-audit.md
- docs/architecture/helper-command-contract.md
- docs/memory/05-tun-and-dns-roadmap.md
- docs/memory/10-roadmap.md
- docs/memory/13-test-strategy-and-legacy-hardening.md
- docs/memory/14-review-findings-and-patchwork-risks.md
- docs/plan/smartx-mihomo-smart-update-plan.md if needed

Docs must say:

- SmartX now has a typed TUN lifecycle diagnostics boundary.
- External-controller TUN remains a guarded controller API patch path.
- Embedded-core TUN remains unsupported.
- Helper-backed TUN remains reserved only.
- System-level TUN verification is not implemented.
- Current verification is controller-config verification only.
- Future work should implement helper-aware runtime preflight, then real route/DNS/interface verification.
- No arbitrary shell or root command runner is allowed for TUN.

============================================================
STRICT VERIFICATION
============================================================

Run all possible checks and do not stop after the first failure.

Required:

git status --short
git diff --stat smartx...HEAD || git diff --stat
bash scripts/codex-lint-scripts.sh
bash scripts/codex-test-focused.sh
bash scripts/codex-diagnose-project.sh

If feasible:

bash scripts/codex-build-debug.sh

Text checks:

grep -R "helper-backed TUN is implemented\|embedded-core TUN is supported\|system-level TUN verification" ClashX docs Tests -n || true
grep -R "route get\|ifconfig\|networksetup\|launchctl\|bash -c\|do shell script" ClashX docs Tests -n || true
grep -R "tunEnable\|tunDisable\|tunStatus\|tunPreflight" ClashX docs Tests -n || true
grep -R "utun\|route\|DNS verification" ClashX docs Tests -n || true

Interpretation:

- TUN command names may appear only as reserved/future/diagnostic model references.
- “system-level TUN verification” may appear only in “not implemented” or future-work wording.
- Existing legacy shell references may remain in legacy docs or legacy helper files, but this PR must not introduce new shell-based TUN behavior.
- Do not treat diagnostics wording as runtime capability.

============================================================
FAILURE HANDLING
============================================================

Do not stop early because one command fails.

If project.pbxproj needs edits:
- add only new production Swift files
- do not modify unrelated build settings

If TunPreflightPlanner standalone smoke compilation is hard:
- keep pure model smoke coverage
- rely on app build for planner integration
- document limitation

If CoreSettingViewController changes are risky:
- skip UI change
- keep diagnostics/report/model changes
- document follow-up

If changing TunLifecycleCoordinator risks breaking behavior:
- keep existing behavior
- only improve typed message/report generation
- document remaining debt

If CI workflow duplicates smoke harness lists:
- update all occurrences

============================================================
FINAL RESPONSE REQUIREMENTS
============================================================

Final response must include:

1. Branch name.
2. Summary of changes.
3. TUN lifecycle boundary summary:
   - external-controller path
   - embedded-core unsupported state
   - helper-backed TUN reserved state
   - verification scope
4. Work ledger:
   - pure models
   - preflight planner
   - coordinator rewiring
   - diagnostics output
   - Core Settings messaging
   - smoke harness / CI
   - docs sync
5. Verification table:
   - command
   - result
   - note
6. Known limitations.
7. Suggested PR title.
8. Suggested PR body.

Do not claim:

- helper-backed TUN support
- embedded-core TUN support
- route verification
- DNS runtime verification
- utun creation
- full system-level TUN verification
- signing/notarization readiness
- release readiness
- high test coverage