You are working in the YaoYinYing/SmartX repository.

Create one bounded PR from the latest smartx branch.

Suggested branch name:

codex/helper-boundary-audit

Suggested PR title:

Audit helper boundaries and add fail-closed diagnostics for future TUN work

This PR is the first step toward real TUN/helper work, but it must NOT implement full TUN support.

Primary goal:

Make the privileged helper boundary explicit, auditable, and fail-closed before adding any new TUN runtime capability.

SmartX currently inherits historical ClashX helper behavior. Before TUN work can proceed, we need to know:

- what the helper currently does
- what requires privilege
- what is legacy proxy setup
- what is safe to keep
- what must be guarded
- how unsigned/debug builds should fail
- how the UI and diagnostics should explain helper unavailability

This PR should reduce ambiguity, not expand privilege.

Hard non-goals:

- Do NOT implement embedded-core TUN.
- Do NOT implement full helper-based TUN install/enable/disable.
- Do NOT add arbitrary root shell execution.
- Do NOT add a generic sudo/helper command runner.
- Do NOT change signing/notarization assumptions.
- Do NOT rename bundle IDs, helper IDs, launchd labels, or app identity in this PR.
- Do NOT rewrite the helper from scratch.
- Do NOT add a full TUN settings UI.
- Do NOT add new controller endpoints.
- Do NOT change proxy behavior unless needed for diagnostics or fail-closed safety.
- Do NOT claim release readiness.

Before editing, inspect these files and related references:

- ClashX.xcodeproj/project.pbxproj
- ClashX/General/HelperTool.swift
- ClashX/General/HelperToolManager.swift
- ClashX/General/Utils/PrivilegedHelperManager.swift
- ClashX/General/Utils/LaunchAtLoginHelper.swift
- ClashX/General/Managers/TunLifecycleCoordinator.swift
- ClashX/ViewControllers/Settings/CoreSettingViewController.swift
- ClashX/ViewControllers/Connections/DiagnosticsDashboardViewController.swift
- ClashX/General/Utils/DiagnosticsReportBuilder.swift
- ClashX/General/Utils/DiagnosticsBundleExporter.swift
- ClashX/General/Managers/CoreCapability.swift
- ClashX/General/Managers/CoreCapabilityProbe.swift
- ClashX/Macro/Paths.swift
- scripts/check_helper_requirement.py
- .github/workflows/pr-ci.yml
- docs/memory/05-tun-and-dns-roadmap.md
- docs/memory/10-roadmap.md
- docs/memory/13-test-strategy-and-legacy-hardening.md
- docs/memory/14-review-findings-and-patchwork-risks.md
- docs/plan/smartx-mihomo-smart-update-plan.md

If exact file names differ, search for:

- SMJobBless
- privileged helper
- helper
- launchd
- Authorization
- com.west2online.ClashX.ProxyConfigHelper
- HelperTool
- blessed
- requirement
- proxy config
- TUN

Run initial discovery:

git status --short
git branch --show-current
git log --oneline --decorate -8
grep -R "SMJobBless\\|Authorization\\|HelperTool\\|ProxyConfigHelper\\|privileged helper\\|launchd\\|tun" ClashX scripts docs .github -n || true

Then complete the following targets.

============================================================
TARGET 1 — Create a helper boundary inventory
============================================================

Create a new document:

docs/architecture/helper-boundary-audit.md

If docs/architecture does not exist, create it.

The document must describe the current privileged helper boundary.

Include:

1. Current helper identity:
   - helper bundle identifier
   - launchd label if discoverable
   - executable name
   - app-side manager classes
   - requirement-check script if present

2. Current helper responsibilities:
   - proxy configuration
   - network service / system proxy behavior
   - install/uninstall or bless flow if present
   - any cleanup/reset behavior
   - any TUN-related behavior, if any currently exists

3. Privilege classification:
   - privileged and necessary
   - privileged but legacy/unclear
   - non-privileged and should stay in app
   - future TUN-related but not implemented

4. Risk inventory:
   - generic command execution risk
   - unsigned debug build behavior
   - stale ClashX bundle/helper identity risk
   - launchd label mismatch risk
   - helper requirement mismatch risk
   - silent helper failure risk
   - TUN preflight ambiguity risk

5. Current decision:
   SmartX must treat helper availability as a capability with explicit diagnostics.
   SmartX must fail closed when helper identity, requirement, or install status cannot be trusted.

6. Future PR boundary:
   - PR14: audit and diagnostics only
   - PR15: helper command contract
   - PR16: helper-aware TUN preflight
   - PR17: TUN runtime verification and recovery

Do not overclaim implemented behavior.

============================================================
TARGET 2 — Add helper status model
============================================================

Add a small pure Swift model for helper status if no equivalent exists.

Suggested file:

ClashX/Models/HelperStatus.swift

or another location consistent with project style.

The model should be Foundation-only if possible.

Suggested structure:

enum HelperTrustState: String, Codable {
    case unknown
    case unavailable
    case unsignedDebugBuild
    case requirementMismatch
    case notInstalled
    case installedButUnverified
    case verified
}

struct HelperStatus: Codable {
    var trustState: HelperTrustState
    var isPrivilegedHelperAvailable: Bool
    var bundleIdentifier: String?
    var launchdLabel: String?
    var requirementSummary: String?
    var lastCheckedAt: Date?
    var diagnosticMessage: String
    var recoverySuggestion: String
}

Rules:

- Keep it descriptive.
- Do not make it perform privileged actions.
- Do not query launchd directly here if that belongs in a manager.
- Make it usable by diagnostics and UI.

Add to Xcode project target if needed.

============================================================
TARGET 3 — Add helper diagnostics probe without changing helper behavior
============================================================

Add or update a helper diagnostics component.

Suggested file:

ClashX/General/Managers/HelperDiagnosticsProbe.swift

or:

ClashX/General/Utils/HelperDiagnosticsProbe.swift

It should collect safe, read-only helper diagnostics.

It may check:

- whether app is unsigned/debug build, if this can be detected safely
- whether helper manager reports installed/available
- known bundle id / launchd label constants
- whether requirement-check script logic has an app-side equivalent
- whether helper status is unknown because runtime probing is unavailable

It must NOT:

- install the helper
- uninstall the helper
- bless the helper
- start/stop launchd services
- mutate system proxy settings
- run arbitrary shell commands
- request admin authorization just to probe
- enable TUN

If safe runtime detection is not available, return `unknown` or `installedButUnverified` with an honest message.

The point is to create a diagnostic boundary, not to force a result.

============================================================
TARGET 4 — Surface helper status in diagnostics
============================================================

Update diagnostics output so helper state is visible.

Inspect and update:

- DiagnosticsReportBuilder
- DiagnosticsBundleExporter if relevant
- DiagnosticsDashboardViewController if low-risk
- CoreCapability / CoreCapabilityProbe if it naturally fits

Requirements:

1. Add a “Privileged Helper” or “Helper Status” section to diagnostics report.
2. Include:
   - trust state
   - availability
   - bundle id / launchd label if known
   - diagnostic message
   - recovery suggestion
3. Do not include secrets.
4. Do not include raw authorization tokens.
5. Do not trigger privilege prompts during diagnostics export.
6. If helper status cannot be checked, say so explicitly.

Optional UI:
If low-risk, add a read-only helper status line in Diagnostics Dashboard.
Do not add helper install/uninstall buttons in this PR.

============================================================
TARGET 5 — Make TUN preflight helper-aware but not helper-powered
============================================================

Update TunLifecycleCoordinator only enough to make its current behavior more honest.

Requirements:

1. TUN preflight should include helper status if available.
2. If current architecture does not support embedded-core TUN, keep blocking it.
3. If external-controller TUN still works through controller config patching, do not break it.
4. If helper is unavailable, do not automatically block all external-controller TUN unless the current path truly needs helper.
5. Add clear messaging:
   - “Helper status is unknown; embedded TUN is not supported in this build.”
   - “External controller TUN can only be attempted through the controller API.”
   - “Future helper-backed TUN requires a separate command contract.”
6. Do not implement helper-backed TUN.

The result should make future TUN work safer without adding runtime privilege changes.

============================================================
TARGET 6 — Add fail-closed guardrails
============================================================

Find any place where helper availability, installation, or requirement mismatch is silently ignored.

Add guardrails where low-risk:

1. If a helper operation fails requirement validation, return a structured failure.
2. If a helper operation is unavailable in unsigned Debug builds, show a clear message.
3. Do not fall back to unsafe behavior.
4. Do not perform shell-based privileged workaround.
5. Do not swallow helper errors without logging or diagnostic state.

This target should be conservative. If changing runtime behavior is risky, add documentation and diagnostics first.

============================================================
TARGET 7 — Tests / smoke harness / scripts
============================================================

Add focused test coverage for pure helper models and diagnostics logic.

Suggested harness:

Tests/SecurityHarness/helper_status_smoke.swift

It should verify:

- HelperStatus Codable round trip
- trust state names
- unsignedDebugBuild / requirementMismatch / verified diagnostic messages if model helpers exist
- no privileged action is needed

Update:

scripts/codex-test-focused.sh

Add the helper status smoke harness.

If the harness compiles production code, include the production model file in the swiftc command.

Do not copy production model structs into the harness.

Update:

Tests/README.md

Document that helper tests are diagnostic/model smoke tests only, not real helper installation tests.

============================================================
TARGET 8 — Documentation sync
============================================================

Update memory docs:

- docs/memory/05-tun-and-dns-roadmap.md
- docs/memory/10-roadmap.md
- docs/memory/13-test-strategy-and-legacy-hardening.md
- docs/memory/14-review-findings-and-patchwork-risks.md
- docs/plan/smartx-mihomo-smart-update-plan.md if needed

Docs must say:

- helper boundary audit exists
- helper status is diagnostic only
- no helper-backed TUN has been implemented yet
- no embedded-core TUN support is claimed
- TUN work should proceed through helper command contract next
- unsigned/debug builds may fail helper checks by design
- smoke tests do not prove real privileged helper installation

============================================================
STRICT VERIFICATION
============================================================

Run all possible checks and do not stop after the first failure.

Required:

git status --short
git diff --stat smartx...HEAD || git diff --stat
bash scripts/codex-lint-scripts.sh
bash scripts/codex-test-focused.sh
bash scripts/codex-diagnose-project.sh

If feasible:

bash scripts/codex-build-debug.sh

Text checks:

grep -R "SMJobBless\\|Authorization\\|ProxyConfigHelper\\|HelperTool\\|privileged helper" ClashX scripts docs -n || true
grep -R "embedded TUN" ClashX docs -n || true
grep -R "helper-backed TUN" ClashX docs -n || true
grep -R "arbitrary shell\\|sudo\\|/bin/sh\\|bash -c" ClashX -n || true

Interpretation:

- Existing shell usage may exist in non-helper scripts; do not rewrite broadly.
- No new generic privileged shell runner should be introduced.
- “helper-backed TUN” may appear only as future work unless actually implemented.
- “embedded TUN” must not be claimed as supported.

============================================================
FAILURE HANDLING
============================================================

Do not terminate early because one command fails.

If helper files are hard to identify:
- search broadly
- document uncertainty
- still create the audit doc and helper status model

If runtime helper probing is unsafe:
- return unknown / unavailable
- document why
- do not force admin prompts

If Xcode build fails due environment:
- record it
- still run scripts and smoke harnesses

If project.pbxproj needs updates:
- add only the new files
- do not modify unrelated build settings

============================================================
FINAL RESPONSE REQUIREMENTS
============================================================

Final response must include:

1. Branch name.
2. Summary of changes.
3. Helper boundary findings:
   - current helper identity
   - current helper responsibilities
   - current unknowns
4. Work ledger:
   - audit doc
   - helper status model
   - diagnostics probe
   - diagnostics surface
   - TUN preflight messaging
   - fail-closed guardrails
   - tests/docs
5. Verification table:
   - command
   - result
   - note
6. Known limitations.
7. Suggested PR title.
8. Suggested PR body.

Do not claim:

- helper-backed TUN support
- embedded-core TUN support
- full helper rewrite
- signing/notarization readiness
- high test coverage
- release readiness