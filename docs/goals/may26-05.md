You are working in the YaoYinYing/SmartX repository.

Create one bounded PR from the latest smartx branch.

Suggested branch name:

codex/tun-route-dns-readonly-evidence

Suggested PR title:

Add read-only TUN route and DNS runtime evidence

Context:

PR14 audited the privileged helper boundary.
PR15 defined the typed helper command contract and kept helper-backed TUN commands reserved.
PR16 added typed TUN lifecycle preflight and controller-config verification.
PR17 added read-only TUN runtime interface evidence using getifaddrs.

Now implement PR18: add read-only route and DNS runtime evidence.

This PR must remain read-only.

Primary goal:

Extend SmartX TUN runtime diagnostics from:

- controller-config verification
- tun-like interface evidence

to also include conservative, non-privileged, read-only evidence about:

- visible route-table state
- visible DNS runtime state

This PR must help SmartX explain:

- “controller config says tun.enable=true”
- “a tun-like interface is or is not visible”
- “route evidence is available / unavailable / inconclusive”
- “DNS runtime evidence is available / unavailable / inconclusive”

This PR must not mutate network state.

Hard non-goals:

- Do NOT implement helper-backed TUN.
- Do NOT implement embedded-core TUN enablement.
- Do NOT create utun interfaces.
- Do NOT modify routes.
- Do NOT modify DNS.
- Do NOT call `route` as a shell command.
- Do NOT call `ifconfig` as a shell command.
- Do NOT call `networksetup`.
- Do NOT call `scutil`.
- Do NOT call `launchctl`.
- Do NOT add generic shell execution.
- Do NOT add new helper XPC methods.
- Do NOT change ProxyConfigHelper.m.
- Do NOT change ProxyConfigRemoteProcessProtocol.h.
- Do NOT start mihomo through the helper.
- Do NOT change signing/notarization.
- Do NOT claim full runtime verification.
- Do NOT claim packet-flow verification.
- Do NOT claim DNS hijack verification.
- Do NOT add a full TUN settings UI.
- Do NOT change ordinary system proxy behavior.

Acceptable:

- Read-only route inspection using Swift/Darwin APIs if feasible.
- Read-only DNS inspection using SystemConfiguration APIs if feasible.
- Pure model/report generation.
- Conservative diagnostics output.
- Core Settings recovery-message improvement.
- Smoke tests for pure models and deterministic classifiers.
- Returning unavailable / inconclusive where runtime probing is not safe or not available.

Before editing, inspect:

- ClashX/Models/TunLifecycleDiagnostics.swift
- ClashX/General/Utils/TunRuntimeInterfaceProbe.swift
- ClashX/General/Managers/TunPreflightPlanner.swift
- ClashX/General/Managers/TunLifecycleCoordinator.swift
- ClashX/General/Utils/DiagnosticsReportBuilder.swift
- ClashX/ViewControllers/Settings/CoreSettingViewController.swift
- Tests/SecurityHarness/tun_lifecycle_diagnostics_smoke.swift
- Tests/SecurityHarness/tun_runtime_diagnostics_smoke.swift
- scripts/codex-test-focused.sh
- .github/workflows/pr-ci.yml
- docs/architecture/helper-boundary-audit.md
- docs/architecture/helper-command-contract.md
- docs/memory/05-tun-and-dns-roadmap.md
- docs/memory/10-roadmap.md
- docs/memory/13-test-strategy-and-legacy-hardening.md
- docs/memory/14-review-findings-and-patchwork-risks.md
- docs/plan/smartx-mihomo-smart-update-plan.md

Initial discovery:

git status --short
git branch --show-current
git log --oneline --decorate -8
grep -R "TunRuntimeVerificationReport\|TunRuntimeInterfaceProbe\|routeVerificationNotImplemented\|dnsRuntimeVerificationNotImplemented\|packetFlowVerificationNotImplemented\|getifaddrs\|utun\|route\|DNS runtime" ClashX Tests docs scripts -n || true

Then complete the targets below.

============================================================
TARGET 1 — Extend typed runtime diagnostics models
============================================================

Extend:

ClashX/Models/TunLifecycleDiagnostics.swift

or add a new pure model file if cleaner:

ClashX/Models/TunRouteDNSRuntimeDiagnostics.swift

Define typed route and DNS evidence models.

Suggested types:

enum TunRouteEvidenceState: String, Codable {
    case notChecked
    case unavailable
    case noTunLikeRouteEvidence
    case tunLikeRouteEvidencePresent
    case inconclusive
}

struct TunRouteRuntimeEvidence: Codable {
    var evidenceState: TunRouteEvidenceState
    var defaultRouteInterface: String?
    var tunLikeRouteInterfaces: [String]
    var observedRouteInterfaces: [String]
    var message: String
}

enum TunDNSRuntimeEvidenceState: String, Codable {
    case notChecked
    case unavailable
    case noDNSRuntimeEvidence
    case dnsRuntimeEvidencePresent
    case inconclusive
}

struct TunDNSRuntimeEvidence: Codable {
    var evidenceState: TunDNSRuntimeEvidenceState
    var resolverInterfaceNames: [String]
    var resolverServerCount: Int?
    var hasScopedResolvers: Bool?
    var message: String
}

Update:

struct TunRuntimeVerificationReport

to include optional or required fields:

    var routeEvidence: TunRouteRuntimeEvidence
    var dnsEvidence: TunDNSRuntimeEvidence

Rules:

1. Models must be Codable.
2. Models must be pure.
3. No AppKit.
4. No shell.
5. No helper calls.
6. No route mutation.
7. No DNS mutation.
8. No claim of packet-flow verification.
9. Route and DNS evidence must remain evidence, not proof.

Add any new model file to the app target.

============================================================
TARGET 2 — Add read-only route evidence probe
============================================================

Add a read-only route evidence probe.

Suggested path:

ClashX/General/Utils/TunRuntimeRouteProbe.swift

Responsibility:

- collect minimal read-only route/interface evidence
- identify whether route-visible interfaces include tun-like names
- avoid shell commands
- avoid route mutation

Preferred implementation choices:

Option A, if feasible:
- use Darwin sysctl routing table APIs in read-only mode
- avoid shell command fallback

Option B, if sysctl route parsing is risky:
- implement pure classification helpers now
- return `.unavailable` from host probing with an honest message
- document route runtime probing as unavailable until a safe parser lands

Allowed testable pure helper:

static func classify(defaultRouteInterface: String?,
                     observedRouteInterfaces: [String],
                     tunLikeInterfaceNames: [String]) -> TunRouteRuntimeEvidence

Classifier behavior:

- if no route interfaces are provided:
  - `.inconclusive`
- if observed route interfaces contain tun-like interface names:
  - `.tunLikeRouteEvidencePresent`
- if default route interface is tun-like:
  - `.tunLikeRouteEvidencePresent`
- if route interfaces are present but none are tun-like:
  - `.noTunLikeRouteEvidence`
- message must say route evidence is not packet-flow proof

Do NOT call:
- route
- netstat
- ifconfig
- networksetup
- scutil
- bash
- sh

============================================================
TARGET 3 — Add read-only DNS runtime evidence probe
============================================================

Add a read-only DNS evidence probe.

Suggested path:

ClashX/General/Utils/TunRuntimeDNSProbe.swift

Responsibility:

- inspect DNS runtime evidence conservatively
- prefer SystemConfiguration read-only APIs if feasible
- avoid shell commands
- avoid DNS mutation

Preferred implementation:

- use SystemConfiguration APIs such as SCDynamicStoreCopyDNS if feasible
- collect only minimal safe summary:
  - resolver server count
  - scoped resolver interface names if available
  - whether scoped resolvers exist
- do not dump all DNS server IPs into diagnostics unless clearly needed and safe
- avoid secrets and private network dumps

If SystemConfiguration implementation is too risky:

- implement pure classification helpers now
- return `.unavailable` from host probing with an honest message
- document DNS runtime probing as unavailable until a safe parser lands

Allowed testable pure helper:

static func classify(resolverInterfaceNames: [String],
                     resolverServerCount: Int?,
                     hasScopedResolvers: Bool?) -> TunDNSRuntimeEvidence

Classifier behavior:

- no data:
  - `.inconclusive`
- scoped resolvers or resolver interface names present:
  - `.dnsRuntimeEvidencePresent`
- server count exists but no scoped interface:
  - `.dnsRuntimeEvidencePresent` but message must say this is generic DNS evidence, not TUN DNS proof
- explicit zero server count:
  - `.noDNSRuntimeEvidence`
- message must say DNS runtime evidence is not DNS hijack proof

Do NOT call:
- scutil
- networksetup
- dig
- nslookup
- bash
- sh

============================================================
TARGET 4 — Integrate route/DNS evidence into runtime verification report
============================================================

Update:

- TunPreflightPlanner.runtimeVerificationReport(...)
- TunPreflightPlanner.verificationReport(...)
- TunLifecycleCoordinator.verifyTunState(...)

Current PR17 behavior attaches interface evidence.

Extend it to also attach:

- route evidence
- DNS runtime evidence

Use read-only probes:

- TunRuntimeInterfaceProbe.currentEvidence()
- TunRuntimeRouteProbe.currentEvidence(...)
- TunRuntimeDNSProbe.currentEvidence(...)

If route/DNS host probing returns unavailable, still include that state in the report.

Behavior:

1. Controller config verification remains primary implemented verification.
2. Interface evidence remains supporting evidence.
3. Route evidence remains supporting evidence.
4. DNS runtime evidence remains supporting evidence.
5. Packet-flow verification remains not implemented.
6. Do not convert route/DNS evidence absence into hard failure.
7. Do not convert route/DNS evidence presence into full success.
8. Message must stay conservative.

Example message patterns:

- “Controller config matches tun.enable=true. Tun-like interface evidence is present. Route evidence is inconclusive. DNS runtime evidence is present, but DNS hijack verification is not implemented. Packet-flow verification is not implemented.”
- “Controller config matches tun.enable=true, but no tun-like interface or route evidence was observed. This does not prove failure; inspect the core log and current controller state.”
- “Controller config matches tun.enable=false, but tun-like interface or route evidence remains. It may belong to another app or require manual inspection.”

============================================================
TARGET 5 — Update diagnostics report
============================================================

Update DiagnosticsReportBuilder.

Extend the TUN runtime section to include:

- interface evidence state
- tun-like interface names
- route evidence state
- default route interface if known
- tun-like route interface names
- DNS runtime evidence state
- resolver interface names if known
- resolver server count if known
- scoped resolver presence if known
- verification levels
- message
- recovery suggestion

Must explicitly state:

- route evidence is read-only and not packet-flow proof
- DNS runtime evidence is read-only and not DNS hijack proof
- packet-flow verification is not implemented
- helper-backed TUN remains reserved only
- embedded-core TUN remains unsupported

Rules:

1. No secrets.
2. No shell commands.
3. No helper prompts.
4. No controller mutation.
5. No route mutation.
6. No DNS mutation.
7. Do not dump full network configuration unless minimal and safe.
8. Interface names are okay.
9. Avoid raw IP address dumps unless redacted or necessary.

============================================================
TARGET 6 — Improve Core Settings recovery text without expanding UI
============================================================

Update CoreSettingViewController only if low-risk.

When TUN toggle completes, include short route/DNS evidence summaries in the existing notification/recovery message.

Do not add a full TUN panel.

Examples:

- “Controller config matches tun.enable=true. Interface evidence: utun4. Route evidence: inconclusive. DNS runtime evidence: scoped resolver present. Packet-flow verification is not implemented.”
- “Controller config matches tun.enable=true, but no tun-like interface or route evidence was observed. This does not prove failure; inspect the core log and current controller state.”

Rules:

1. No new buttons.
2. No helper install button.
3. No route/DNS check button.
4. No privileged action.
5. Do not block toggle success only because route/DNS evidence is inconclusive.

============================================================
TARGET 7 — Add smoke harness coverage
============================================================

Add:

Tests/SecurityHarness/tun_route_dns_runtime_smoke.swift

or extend:

Tests/SecurityHarness/tun_runtime_diagnostics_smoke.swift

Prefer a new harness if the file gets too large.

It should compile production pure model files and deterministic classifiers.

Test route classifier:

1. defaultRouteInterface nil, observed [] -> inconclusive
2. observed ["en0"] + tunLike ["utun4"] -> noTunLikeRouteEvidence
3. observed ["en0", "utun4"] + tunLike ["utun4"] -> tunLikeRouteEvidencePresent
4. defaultRouteInterface "utun4" -> tunLikeRouteEvidencePresent
5. message must not claim packet-flow verification

Test DNS classifier:

1. no interface names, nil server count, nil scoped -> inconclusive
2. resolverInterfaceNames ["utun4"] -> dnsRuntimeEvidencePresent
3. resolverServerCount 2, no scoped interface -> dnsRuntimeEvidencePresent but not TUN DNS proof
4. resolverServerCount 0 -> noDNSRuntimeEvidence
5. message must not claim DNS hijack verification

Test runtime report:

1. expectedEnabled true + controllerReported true + interface present + route present + DNS present:
   - report mentions evidence but does not claim full runtime verification
2. expectedEnabled true + controllerReported true + no interface + no route:
   - report says this does not prove failure
3. verification levels include:
   - controllerConfigOnly
   - interfacePresenceOnly when interface evidence was checked
   - routeVerificationNotImplemented or a read-only route evidence level if you add one
   - dnsRuntimeVerificationNotImplemented or a read-only DNS evidence level if you add one
   - packetFlowVerificationNotImplemented
4. no report text claims helper-backed TUN is implemented

Update:

- scripts/codex-test-focused.sh
- .github/workflows/pr-ci.yml
- Tests/README.md

Do not copy production structs into harnesses.

============================================================
TARGET 8 — Documentation sync
============================================================

Update:

- docs/architecture/helper-boundary-audit.md
- docs/architecture/helper-command-contract.md
- docs/memory/05-tun-and-dns-roadmap.md
- docs/memory/10-roadmap.md
- docs/memory/13-test-strategy-and-legacy-hardening.md
- docs/memory/14-review-findings-and-patchwork-risks.md
- docs/plan/smartx-mihomo-smart-update-plan.md if needed

Docs must say:

- SmartX now has read-only interface, route, and DNS runtime evidence.
- Interface evidence is not proof of packet routing.
- Route evidence is not packet-flow proof.
- DNS runtime evidence is not DNS hijack proof.
- Packet-flow verification is not implemented.
- Helper-backed TUN remains reserved only.
- Embedded-core TUN remains unsupported.
- No privileged or shell-based TUN probing was added.
- Future work should decide whether to implement helper-backed TUN commands under the typed helper contract.

============================================================
STRICT VERIFICATION
============================================================

Run all possible checks and do not stop after the first failure.

Required:

git status --short
git diff --stat smartx...HEAD || git diff --stat
bash scripts/codex-lint-scripts.sh
bash scripts/codex-test-focused.sh
bash scripts/codex-diagnose-project.sh

If feasible:

bash scripts/codex-build-debug.sh

Text checks:

grep -R "helper-backed TUN is implemented\|embedded-core TUN is supported\|full runtime verification\|packet-flow verification succeeded\|DNS hijack verified\|route verification succeeded" ClashX docs Tests -n || true
grep -R "route get\|ifconfig \|networksetup\|launchctl\|bash -c\|do shell script\|scutil\|netstat" ClashX docs Tests -n || true
grep -R "getifaddrs\|SCDynamicStoreCopyDNS\|TunRuntimeRouteProbe\|TunRuntimeDNSProbe\|TunRuntimeVerificationReport" ClashX docs Tests -n || true

Interpretation:

- getifaddrs is allowed for read-only interface enumeration.
- SystemConfiguration DNS read-only APIs are allowed.
- route/DNS evidence must be framed as evidence, not proof.
- packet-flow verification may appear only as not implemented or future work.
- Existing legacy shell references may remain, but this PR must not introduce new shell-based TUN probing.

============================================================
FAILURE HANDLING
============================================================

Do not stop early because one command fails.

If route-table parsing is risky:
- keep pure route classifier
- return unavailable from host route probe
- document limitation
- do not use shell fallback

If DNS SystemConfiguration probing is risky:
- keep pure DNS classifier
- return unavailable from host DNS probe
- document limitation
- do not use shell fallback

If project.pbxproj needs edits:
- add only new Swift files
- do not modify unrelated build settings

If coordinator integration becomes risky:
- keep route/DNS evidence in diagnostics only
- document coordinator integration as follow-up
- do not destabilize external-controller patch behavior

If Core Settings UI change becomes risky:
- skip UI change
- keep diagnostics/report/model changes
- document follow-up

If CI workflow has duplicate smoke harness lists:
- update every relevant occurrence

============================================================
FINAL RESPONSE REQUIREMENTS
============================================================

Final response must include:

1. Branch name.
2. Summary of changes.
3. Runtime evidence boundary summary:
   - controller config verification
   - interface evidence
   - route evidence
   - DNS runtime evidence
   - packet-flow verification status
4. Work ledger:
   - pure models
   - route probe
   - DNS probe
   - coordinator integration
   - diagnostics output
   - Core Settings messaging
   - smoke harness / CI
   - docs sync
5. Verification table:
   - command
   - result
   - note
6. Known limitations.
7. Suggested PR title.
8. Suggested PR body.

Do not claim:

- helper-backed TUN support
- embedded-core TUN support
- route mutation
- DNS mutation
- DNS hijack verification
- packet-flow verification
- utun creation
- full system-level TUN verification
- signing/notarization readiness
- release readiness
- high test coverage