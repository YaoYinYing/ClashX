You are working in YaoYinYing/SmartX on PR #18 / branch codex/tun-route-dns-readonly-evidence.

Do not expand scope. This is a final review-polish pass for PR #18.

Context:

PR18 adds read-only TUN route/DNS runtime evidence.

It also intentionally carries forward the PR17 review fix: passive diagnostics/status surfaces must not synthesize post-toggle verification by passing the same cached tun.enable value as both expectedEnabled and controllerReportedEnabled.

This PR17 follow-up is in scope for PR18 because route/DNS runtime evidence is part of the same passive-vs-post-toggle diagnostics boundary.

Keep the passive snapshot fix.

Do not revert:

- TunPreflightPlanner.passiveRuntimeSnapshotReport(...)
- DiagnosticsReportBuilder using passiveRuntimeSnapshotReport(...)
- CoreSettingViewController using passiveRuntimeSnapshotReport(...)

Primary goal:

Make the passive snapshot semantics strict and unambiguous:

- passive diagnostics/status output may describe cached/current controller config
- passive diagnostics/status output may describe read-only interface/route/DNS evidence
- passive diagnostics/status output must not claim post-toggle verification
- passive diagnostics/status output must not claim full runtime verification
- real post-toggle verification must remain limited to TunLifecycleCoordinator.verifyTunState(...)

============================================================
TARGET 1 — Keep passive snapshot, but make it non-judgmental
============================================================

Inspect:

- ClashX/General/Managers/TunPreflightPlanner.swift
- ClashX/General/Utils/DiagnosticsReportBuilder.swift
- ClashX/ViewControllers/Settings/CoreSettingViewController.swift

Current expected shape:

- DiagnosticsReportBuilder.tunLifecycleBoundarySection() should call passiveRuntimeSnapshotReport(...)
- CoreSettingViewController.tunCapabilityNoteText(config:) should call passiveRuntimeSnapshotReport(...)
- TunLifecycleCoordinator.verifyTunState(...) should keep using the real post-toggle verification path

Required behavior:

1. passiveRuntimeSnapshotReport(...) must never produce “Controller config matches...”

2. passiveRuntimeSnapshotReport(...) must never imply:
   - verified
   - succeeded
   - post-toggle verified
   - runtime consistent
   - full runtime verification

3. passiveRuntimeSnapshotReport(...) should say, in plain text:
   - this is a passive diagnostics snapshot
   - this is not a post-toggle verification
   - cached/current tun.enable may be stale
   - interface/route/DNS evidence is read-only evidence only
   - packet-flow verification is not implemented

4. passiveRuntimeSnapshotReport(...) should preserve:
   - controllerReportedEnabled as cached/current snapshot state, if available
   - interfaceEvidence
   - routeEvidence
   - dnsEvidence
   - verificationLevels

5. For passive reports, set:

   isRuntimeConsistentWithController = nil

Do not compute runtimeConsistency(...) for passive reports.

Runtime consistency is only meaningful in the real post-toggle verification path where:

- expectedEnabled comes from the user’s requested toggle state
- controllerReportedEnabled comes from a fresh controller config read after PATCH /configs

============================================================
TARGET 2 — Preserve real post-toggle verification
============================================================

Inspect:

- ClashX/General/Managers/TunLifecycleCoordinator.swift
- ClashX/General/Managers/TunPreflightPlanner.swift

Do not weaken the real verification path.

TunLifecycleCoordinator.verifyTunState(...) should continue to:

1. reload controller config after a successful controller patch
2. pass the user-requested expectedEnabled value
3. pass the freshly read config.tun?.enable value as controllerReportedEnabled
4. use runtimeVerificationReport(...) or verificationReport(...)
5. include read-only interface/route/DNS evidence as supporting evidence

Allowed real post-toggle wording:

- “Controller config matches tun.enable=true.”
- “Controller config matches tun.enable=false.”
- “SmartX requested tun.enable=true, but the controller reported tun.enable=false.”

Forbidden only in passive snapshot contexts.

============================================================
TARGET 3 — Make tests explicitly protect the boundary
============================================================

Update or add tests in:

- Tests/SecurityHarness/tun_runtime_diagnostics_smoke.swift
- Tests/SecurityHarness/tun_route_dns_runtime_smoke.swift

Add assertions for passiveRuntimeSnapshotReport(...):

1. message contains “passive diagnostics snapshot”
2. message contains “not a post-toggle verification”
3. message does not contain “Controller config matches”
4. message does not contain “verified”
5. message does not contain “succeeded”
6. isRuntimeConsistentWithController == nil
7. interfaceEvidence is preserved
8. routeEvidence is preserved
9. dnsEvidence is preserved
10. verificationLevels include packetFlowVerificationNotImplemented
11. message does not claim helper-backed TUN support
12. message does not claim embedded-core TUN support

Also keep or add tests for real post-toggle runtimeVerificationReport(...) where “Controller config matches...” is allowed.

The tests should prove:

- passive snapshot = evidence-only
- post-toggle verification = request/result comparison after reload

Do not copy production structs into harnesses.

============================================================
TARGET 4 — Confirm PR description matches the intended scope
============================================================

PR18 is allowed to include the PR17 passive snapshot follow-up.

The PR body should explicitly say:

- this PR adds read-only route/DNS evidence
- this PR also includes the PR17 passive snapshot follow-up
- passive diagnostics/status output is not post-toggle verification
- real post-toggle verification remains in TunLifecycleCoordinator.verifyTunState(...)
- route/DNS evidence is evidence, not proof

Do not describe passive snapshot work as unrelated.

If the PR body already says this clearly, do not change it.

============================================================
TARGET 5 — Avoid unrelated scope expansion
============================================================

Do not add new unrelated changes.

This PR should remain centered on:

- route/DNS runtime evidence
- passive snapshot follow-up from PR17
- diagnostics/Core Settings wording
- smoke harness and CI wiring
- narrow docs sync

Do not add new work on:

- dashboard branding
- dashboard asset replacement
- dependency installer behavior
- full goals-document rewrites
- helper XPC
- ProxyConfigHelper.m
- signing/notarization
- embedded-core TUN
- helper-backed TUN
- route mutation
- DNS mutation
- packet-flow verification

If unrelated files are already changed and not required for this PR, either revert them or explain why they must remain.

============================================================
VERIFICATION
============================================================

Run:

git diff --stat smartx...HEAD || git diff --stat
bash scripts/codex-lint-scripts.sh
bash scripts/codex-test-focused.sh
bash scripts/codex-diagnose-project.sh

If feasible:

bash scripts/codex-build-debug.sh

Text checks:

grep -R "Controller config matches" ClashX Tests docs -n || true
grep -R "passive diagnostics snapshot\\|post-toggle verification" ClashX Tests docs -n || true
grep -R "Runtime Consistency With Controller" ClashX Tests docs -n || true
grep -R "helper-backed TUN is implemented\\|embedded-core TUN is supported\\|full runtime verification\\|packet-flow verification succeeded\\|DNS hijack verified\\|route verification succeeded" ClashX docs Tests -n || true
grep -R "route get\\|ifconfig \\|networksetup\\|launchctl\\|bash -c\\|do shell script\\|scutil\\|netstat" ClashX docs Tests -n || true

Interpretation:

- “Controller config matches” is allowed only in real post-toggle verification code/tests.
- Passive diagnostics/status code must not produce “Controller config matches”.
- Passive reports must have isRuntimeConsistentWithController == nil.
- route/DNS evidence must remain evidence, not proof.
- No shell-based route/DNS probing should be introduced.

============================================================
FINAL RESPONSE REQUIREMENTS
============================================================

Final response must include:

1. files changed
2. whether passiveRuntimeSnapshotReport(...) now always returns isRuntimeConsistentWithController == nil
3. whether DiagnosticsReportBuilder uses passive snapshot output
4. whether CoreSettingViewController uses passive snapshot output
5. whether TunLifecycleCoordinator still preserves real post-toggle verification
6. whether PR18 body already documents the PR17 follow-up
7. whether any unrelated files remain changed and why
8. verification command results
9. known limitations

Do not claim:

- full runtime verification
- route verification success
- DNS hijack verification
- packet-flow verification
- helper-backed TUN support
- embedded-core TUN support
- signing readiness
- release readiness